# History

> Part of odysseus/.project-knowledge/ | Last updated: 2026-08-04
> Past-only. Append-only — never delete entries.

## Fixed

- **No backup/restore tooling for `data/`** — `scripts/odysseus-backup` now exists: stdlib-only Python, `snapshot`/`list`/`verify`/`restore` subcommands, uses SQLite's own `.backup` API (safe with the app running), excludes `deep_research/` and `mail-attachments/` by default (`--include-research`/`--include-attachments` to include them). Restore renames the existing `data/` to `data.before-restore-<timestamp>` before extracting, and validates archive entries against path traversal/symlinks/absolute paths. Docker users must run it on the **host**, not in-container (`backups/` isn't a mounted volume); Docker's ChromaDB lives in a separate `chromadb-data` volume not covered by the snapshot and needs its own backup. Resolves the long-standing "backup/restore guide and helper" backlog item from `ROADMAP.md`. *(added: upstream, docs: `docs/backup-restore.md`)*
- **Security-CI branch protection** — documented as a completed, available system (not just a roadmap item): gitleaks/actionlint/zizmor/dependency-review/hadolint block merges, pip-audit/Trivy/CodeQL are advisory, CODEOWNERS review required. Gotcha: don't add a checked-in CodeQL workflow while GitHub's default code-scanning setup is active — they conflict. *(docs: `docs/security-ci.md`, see [[systems]])*
- **Deep research: `FINAL_REPORT_PROMPT` template override buried beneath essay defaults** — The "magazine-quality article" format requirements appeared before the "if user has a custom template, override these" check. Moved STRUCTURE CHECK to the top of `FINAL_REPORT_PROMPT`, before any format instructions. Default requirements are now explicitly labelled as fallbacks. *(fixed: 2026-06-12)*
- **Deep research: `_classify_category` returns `None` for "general" category** — When the classifier answered "general", the method returned `None` (because `"general" not in CATEGORY_PROMPTS`). Changed to return `"general"` so `self.category` is always set. *(fixed: 2026-06-12)*
- **Deep research: `SYNTHESIZE_PROMPT` lacked template-awareness** — Synthesis prompt used essay-format language ("well-organized report with logical flow") and had no instruction to preserve user's custom output structure across rounds. Added template-preservation instruction. *(fixed: 2026-06-12)*
- **Deep research: `FINAL_REPORT_PROMPT` fallback instruction didn't require template compliance** — The "write from trained knowledge" instruction didn't tell the model to also apply the user's structural template. Extended to say "AND apply the structural template from the question above." *(fixed: 2026-06-12)*
- **Deep research: `STOP_PROMPT` didn't evaluate structural completeness** — Stop criteria only checked topical coverage, not whether the user's template requirements (entry count, per-entry sections) were met. Added structural completeness criteria. *(fixed: 2026-06-12)*
- **Deep research: `QUERY_GEN_PROMPT` domain example was Batman-specific** — Hardcoded "Batman" example didn't generalise to other subjects. Replaced with domain-agnostic `[character name]` example. *(fixed: 2026-06-12)*
- **Deep research: `_final_report` unreachable when synthesis fails** — When synthesis timed out, `report` stayed `""`, triggering early return via `_fallback_report` — bypassing `_final_report` entirely where the trained-knowledge fallback and template instructions live. Changed to format findings and pass them to `_final_report` instead of exiting early. *(fixed: 2026-06-12)*
- **Deep research: `_synthesize` timing out on long prompts** — Full 500+ word user prompt (with template instructions) was passed as `{question}` to every synthesis call, making prompts too large for `big-pickle` model within 180s timeout. Changed to pass `self.research_plan` (condensed summary, ~70% shorter) instead. *(fixed: 2026-06-12)*
- **Deep research: `_fallback_report` used full question as H1** — `_fallback_report` dumped the entire 500+ word prompt as the document's H1 heading. Changed to use only first sentence, truncated to 120 chars. *(fixed: 2026-06-12)*
- **Deep research: User's process instructions leaking into extractor** — `goal=question` passed the full user prompt (including "Your first task is discovery, not confirmation") into `EXTRACTOR_SYSTEM`, causing the extractor model to reason about the goal instead of extracting content. Changed to pass `self.research_plan or question[:200]` instead. *(fixed: 2026-06-12)*
- **Deep research: Thinking tags leaking into extraction findings** — Model output "Thinking. 1. Analyze the Request:" before JSON, not caught by `strip_thinking` (which handles `<think>` tags but not numbered reasoning format). Added "Do NOT include any reasoning... Output ONLY valid JSON" to `EXTRACTOR_SYSTEM`. *(fixed: 2026-06-12)*
- **OPML import flattened group hierarchy** — `parse_opml` returned a single `group` string per feed (last `<outline>` ancestor only), losing the full nested path. Changed to return `group_path` list (ancestor chain from root to leaf). *(fixed: 2026-06-13)*
- **OPML import didn't create FeedGroup records** — `import_opml` stored `group` as a single string on the feed dict but never created `FeedGroup` rows. Now walks the `group_path`, creates any missing `FeedGroup` records (with `parent_id`), and sets `Feed.group_id`. *(fixed: 2026-06-13)*
- **OPML re-import duplicated feeds** — `import_opml` checked for duplicates by filtering on `Feed.url == raw_xmlUrl` only, but resolved YouTube URLs differ from the OPML's `xmlUrl`. Changed to use explicit `or_()` filter matching both raw `xmlUrl` and resolved URL (when available). *(fixed: 2026-06-13)*
- **RSS feeds had no batch operations** — Multi-select checkboxes missing; each feed action required individual clicks. Added checkboxes on all feed items (flat list + tree), batch action bar with "Move to group" dropdown + "Delete" button, selections cleared on `_loadFeeds`. *(fixed: 2026-06-13)*
- **RSS groups were flat, no nesting** — `FeedGroup` had no `parent_id`; no tree UI. Added self-referential FK, migration, tree-building frontend with collapse/expand, group CRUD with `parent_id`. *(fixed: 2026-06-13)*
- **RSS group collapse toggle too small** — Toggle was 12×8px, hard to click. Enlarged to 18×18px with hover highlight. Changed UX: single-click any area of group header toggles collapse/expand (instead of requiring click on tiny toggle), double-click selects the group and loads its articles. *(fixed: 2026-06-13)*
- **RSS feed checkboxes always visible** — Checkboxes rendered unconditionally, cluttering the sidebar. Added `_selectMode` toggle button ("☐" in filter bar); checkboxes only render when select mode is on. In select mode, clicking any feed item toggles its checkbox instead of navigating. *(fixed: 2026-06-13)*
- **RSS no AI summary for selected group** — No way to batch-summarize all articles in a group. Added `POST /api/feeds/groups/{group_id}/summarize` backend endpoint and "AI Summary" button in article list when a group is selected. *(fixed: 2026-06-13)*
- **`query_llm()` missing from `src/llm_core.py`** — Both `summarize_article` and `summarize_group` endpoints did `from src.llm_core import query_llm` but the function was never defined. Caught in `except Exception` → `{"ok": False, "error": "..."}` → "Failed to generate summary." Added the function as a one-shot wrapper that resolves the task/utility/default model and calls `llm_call()`. *(fixed: 2026-06-19)*
- **RSS group summary returning 504 Gateway Timeout** — Full group prompt (20 articles × 600 chars = ~12 k chars) exceeded the 45 s `REQUEST_HARD_TIMEOUT` on a local model. Reduced in `routes/feed_routes.py`: articles 20→10, content 600→400 chars, prompt cap 12 000→6 000 chars, `max_tokens=600`. *(fixed: 2026-06-19)*
- **Active filter buttons invisible in Navyseal theme** — `--accent-primary` is undefined in Navyseal; `background: var(--accent-primary)` rendered transparent. Fixed with fallback `var(--accent-primary, var(--fg))` + `font-weight: 600`. *(fixed: 2026-06-19)*
- **RSS group-actions hover caused layout shift** — `.rss-group-actions { display: none }` → `display: flex` on hover caused reflow. Fixed to always `display: flex; opacity: 0; pointer-events: none` → fade to `opacity: 1; pointer-events: auto` on hover. *(fixed: 2026-06-19)*
- **Kimi missing from Settings → Add Endpoint dropdown** — The `#adm-epProvider` `<option>` in `index.html` was the only provider list not updated when Kimi was added. *(fixed: 2026-06-19)*
- **Vision images not sent to vision-capable models** — upstream fix *(fixed: upstream, 2026-06-25)*
- **Email tool fences leaked into live chat stream** — executed email tool fences stripped from streaming output. *(fixed: upstream, 2026-06-25)*
- **APIKeyManager used non-atomic write** — credential file could be corrupted on crash mid-write; now uses atomic write. *(fixed: upstream, 2026-06-25)*
- **Agent misfenced `read_file` calls not parsed** — malformed fenced tool calls now recovered. *(fixed: upstream, 2026-06-25)*
- **Task endpoint URL not normalized** — task runner now normalizes endpoint URL to `/chat/completions` before calling the model. *(fixed: upstream, 2026-06-25)*
- **Cookbook blocked launch on any port conflict** — now only blocks on real collisions (same model/port). *(fixed: upstream, 2026-06-25)*
- **`_probe_endpoint` crashed on malformed model-list response** — now hardened against missing/unexpected fields. *(fixed: upstream, 2026-06-25)*
- **Tasks/Skills dropdowns had incorrect z-index** — now routed through `topPortalZ()`. *(fixed: upstream, 2026-06-25)*
- **Email poller didn't mark calendar extraction processed on LLM failure** — silently retried forever on LLM error. *(fixed: upstream, 2026-06-25)*
- **Pre-seeded admin password ignored on native installs** — `.env` wasn't loaded during setup. *(fixed: upstream, 2026-06-25)*
- **Real-ESRGAN + Cookbook deps-panel crash on Python 3.14** — torchvision compat fix. *(fixed: upstream, 2026-06-25)*
- **`src/exceptions` duplicated across `core/` and `src/`** — `src/exceptions` now re-exports from `core`. *(fixed: upstream, 2026-06-25)*

**RSS Feed Reader batch fixes** *(2026-06-13, tracked as completed TODOs)*
- [x] Nested groups — added `parent_id`, tree UI with collapse/expand, group CRUD supports nesting
- [x] OPML import preserves group hierarchy — `parse_opml` returns `group_path`, `import_opml` creates `FeedGroup` records along the path
- [x] OPML import prevents duplicates — matches against both raw and resolved URL via `or_()`
- [x] OPML file upload — `<input type="file">` in OPML modal
- [x] Multi-select feeds — checkboxes + batch action bar with move to group / delete
- [x] Collapse/expand UX — single-click toggles, double-click selects, toggle enlarged 12→18px
- [x] Select mode for checkboxes — "☐" toggle hides checkboxes by default, feed items toggle batch in select mode
- [x] Group AI summary — backend endpoint + frontend button for bullet-point digest of group's articles
- **YouTube AI summary now works** — `feedparser` leaves `content`/`summary` empty for many YouTube RSS entries, so both summarize endpoints had nothing to summarize. Added `_youtube_transcript_text()` (`routes/feed_routes.py`) which detects a YouTube URL and pulls its transcript via `services/youtube/youtube_handler.py` (`extract_transcript_async`, wrapped in `asyncio.run()` since route handlers are sync); falls back to this transcript when `content` is empty, and caches it onto `row.content`. `summarize_group` fetches transcripts for all empty-content YouTube entries concurrently via `asyncio.gather` before building the digest prompt. `fetch_full_content` also branches to transcript fetch for YouTube URLs instead of trafilatura. *(fixed: 2026-07-20)*
- **RSS AI-summary requests were always capped at 45s regardless of prompt size** — root cause was architectural, not prompt-size: `/api/chat` is explicitly exempt from `app.py`'s `REQUEST_HARD_TIMEOUT` middleware ("streaming"), but `/api/feeds` was never added to that exemption list, so summarize requests were killed at 45s no matter what `query_llm(timeout=...)` was set to. An earlier fix session had (incorrectly) treated this as a prompt-size/local-model-speed problem and reduced input to 3000 chars / `timeout=42` — this only reduced how *early* inside the 45s window the failure happened. The real fix: added `/api/feeds/articles` and `/api/feeds/groups` to `app.py`'s `_TIMEOUT_EXEMPT_PREFIXES` (same reasoning as the chat exemption), and raised both endpoints' `query_llm(timeout=...)` to 180s (matching the existing big-pickle/deep-research precedent). Verified live: a request that previously died at 45s now completes in ~126s server-side against a genuinely slow hosted model (`big-pickle` via OpenCode Zen). *(fixed: 2026-07-20)*
- **Reasoning-model chain-of-thought was leaking into (and truncating) RSS summaries** — a model outside `_THINKING_MODEL_PATTERNS` (`src/llm_core.py:1233`, e.g. `big-pickle` via OpenCode Zen) reasons in plain prose with no `<think>` tag or `reasoning_content` field to separate it, so none of `llm_call()`'s existing thinking-model handling (built for Mistral/Qwen3/DeepSeek-R1/etc.) applied — the raw "Thinking. 1. Analyze the Request..." text was returned as the summary itself, and often got cut off mid-thought because `max_tokens` (500/600) was consumed entirely by the reasoning preamble before the model reached an actual answer. Fixed both RSS summarize endpoints with an explicit system-prompt instruction ("Respond with only the final summary/digest — no reasoning, analysis, or commentary about the task itself") and raised `max_tokens` to 1200 as a safety net. This is a per-caller workaround, not a fix in `llm_call()` itself — see [[roadmap]] for the open generic-fix item. *(fixed: 2026-07-20)*
- **3 RSS reader UI bugs fixed**: (1) chat input's model-picker (`z-index: 250`, `position: absolute`) had no ancestor establishing a local stacking context, so it escaped and painted above the RSS/Notes pane backdrops (`z-index: 200`) — lowered to `z-index: 2` since no sibling in that stacking context needed it above 0 anyway; (2) the AI Summary button used a copy-pasted pencil/edit icon instead of an AI-appropriate one — replaced with a sparkle path in all 3 occurrences; (3) clicking AI Summary did nothing visible on failure — old code set `button.textContent` (invisible in a 14px icon button) with no `try/catch`, so a non-JSON error response died silently with the button stuck forever; rewrote with a visible "Generating summary…" loading panel, disabled button state, and `try/catch/finally` with a real error toast. *(fixed: 2026-07-20)*
- **RSS keyboard navigation + Prev/Next buttons added, with a self-inflicted race condition found and fixed** — added `j`/`k` (next/prev article), `m` (mark read), `s` (star) shortcuts, plus toolbar Prev/Next buttons (most users won't discover hidden keybindings). Discovered mid-implementation: opening an article marks it read, which triggers an async background `_loadFeeds()`/`_loadArticles()` refresh of the (filtered) article list — mid-navigation this could reshuffle `_articles` and make "next" jump to an unrelated article, or misreport a Prev-button disabled state. Fixed by snapshotting the list into a stable `_readerNavList` (`.slice()` of `_articles`) when the reader opens from the list view; all nav/star/read/TTS lookups use `_readerNavList.find(...) || _articles.find(...)` instead of the live array, and the snapshot + `_activeArticleId` are both reset to empty in `_closeReader()` (which also makes the shortcuts safely inert once back at the list view, without needing a separate "is reader open" guard). *(fixed: 2026-07-20)*
- **Empty-snippet fallback for video posts with no description** — YouTube Shorts/posts with genuinely no description left a blank line in the article list; now shows italic "▶ No description available" when `content` is empty and the article resolves to a YouTube video ID. *(fixed: 2026-07-20)*
- **Fixed a latent `_el()` selector bug** — a call site passed a compound CSS selector (`.rss-article-item[data-article-id="..."]`) to `_el(id)`, which is defined as `document.getElementById(id)` — silently no-op'd since `getElementById` doesn't match selectors. Switched to `document.querySelector(...)`. *(fixed: 2026-07-20)*
- **RSS sidebar toolbar/group-action buttons read as disabled — they weren't** — `.doc-action-icon-btn` (shared with Notes/Documents toolbars) has a base `opacity: 0.3`; scoped a bump to `0.6` for `.rss-reader-toolbar` only. Group action buttons (add-sub/rename/delete) were a real usability issue though, not just perception — 10px font in a 0×2px padding box, effectively ~14×10px hit targets; enlarged to real 20×20px icon buttons. Same treatment for the "+" add-group and "Select" buttons. *(fixed: 2026-07-20)*
- **RSS group deletion had no UI** — the backend endpoint (`DELETE /api/feeds/groups/{id}`) already existed and safely reparents child groups + ungroups feeds (no cascading delete), it was just never wired to a button. Added one (`.rss-group-delete`) next to the existing add-sub/rename actions. *(fixed: 2026-07-20)*
- **RSS search box and filter/select buttons looked under-styled** — the search input reused the bare `.memory-search-input` base class (24px height, no icon) with none of the scoped overrides other panels (Notes, Email) already apply. Added an RSS-scoped override with a search icon and 32px height, matching the pattern at `.notes-search-bar .memory-search-input`. Filter buttons (Unread/All/Starred) redesigned from 3 separate thin-bordered boxes into a shared segmented-control track. *(fixed: 2026-07-20)*
- **Feed favicons were essentially never populated** — `services/feed/fetcher.py`'s `fetch_feed()` only read Atom's `<icon>`/`<logo>` fields; RSS 2.0's `<image><url>` (what most non-YouTube feeds actually use) maps to `feed.image.href` in feedparser and was never checked, so almost every feed fell back to a plain letter-avatar. Added the `feed.image.href` check plus a last-resort guessed `https://{domain}/favicon.ico`, with an `onerror` handler on the `<img>` (`_feedIconHtml()`) to fall back to the letter avatar if even that guess 404s. Verified live against a real feed (blackhillsinfosec.com) — picked up its actual logo. *(fixed: 2026-07-20)*
- **Group collapse/expand chevron was missing for the common case** — `_renderGroupTreeNode`'s toggle icon only rendered when a group had *sub-groups* (`node.children.length > 0`); groups with feeds-but-no-sub-groups (the typical case) got an empty toggle span even though the header's click handler already collapses/expands regardless of content type. Now shows ▼/▶ whenever a group has feeds or sub-groups. *(fixed: 2026-07-20)*
- **RSS feeds had no way to reorder or move between groups by dragging** — added a `sort_order` column to `Feed` (migration `_migrate_add_feed_sort_order`, following the exact manual-`ALTER TABLE` pattern used for `FeedGroup.parent_id`), changed `GET /api/feeds` to order by `sort_order` then title, and added `sort_order` to `update_feed`'s whitelisted fields. Frontend: a small grip handle (`.rss-feed-drag-handle`) on each feed row wired to the existing shared `dragSortModule` (`static/js/dragSort.js`, already used by `models.js`/`sessions.js`/`galleryEditor.js`). Key insight: the whole feed list already renders as one flat `#rss-feed-list` container with group headers and feed items interleaved by position (not real DOM nesting — see `_buildGroupTree`/`_renderGroupTreeNode`), so dragSort's existing vertical magnetic-reorder handles *both* same-group reordering and cross-group moves via the same gesture — `_onFeedListReordered` just walks the final DOM order, tracking which group header was most recently passed, to infer each feed's new `group_id` + within-group `sort_order`, then persists via the same per-feed `PUT` loop pattern already used by batch-move. Added a synthetic "Ungrouped" divider header (not a real group) so there's an explicit boundary for feeds with no group — without it, ungrouped feeds trailing at the end with no preceding header would be mis-attributed to whichever group rendered last. Verified end-to-end with a real simulated mouse drag (dispatched mousedown/mousemove/mouseup), confirmed correct DB state, then restored the test feed to its original group/position. Known limitation: a collapsed group renders only its header with no feed rows, so there's no droppable space — must expand a group before dropping a feed into it (see [[roadmap]]). *(fixed/added: 2026-07-20)*
- **SSRF via `base_url` on `/api/v1/chat` confirmed already fixed, despite the PR that fixed it being closed unmerged** — roadmap.md had this tracked as "fix proposed in PR #1039, merge status unconfirmed." Checked: `#1039` is CLOSED with `mergedAt: null` (never merged). But equivalent hardening reached `dev` through a different commit path (`b1a4ed1` "Harden API-token chat endpoint selection", plus a revert/reapply cycle `cc8fe2f`→`6861c41`) — `routes/webhook_routes.py:286-292` calls `validate_public_http_url()` (`src/url_security.py:81`) on any user-supplied `base_url` before use, which blocks loopback/RFC1918/link-local/CGNAT/multicast/reserved ranges, `169.254.169.254` (cloud metadata), and `.internal`/`.local`/`.lan` hostnames — via actual DNS resolution, not string matching. `tests/test_api_chat_security.py` has 22 passing tests covering this exact path. Not exploitable on current `dev`. Note: this validation only applies to the *directly user-supplied* `base_url` (chat-scoped tokens) — the separate fallback path that reads admin-configured `ModelEndpoint.base_url` rows is intentionally unvalidated (admins can legitimately point at LAN model servers), which is not the SSRF vector this tracked. *(confirmed fixed: 2026-07-20)*
- **RSS TTS button was calling a method that doesn't exist** — `_playTTS()` called `window.aiTTSManager.speak(content, {title})`; `AITTSManager` (`static/js/tts-ai.js`) only exposes `async play(text)` (single arg) — clicking the button silently did nothing. Fixed the call, wrapped in try/catch with `uiModule.showError` on failure. *(fixed: 2026-07-20)*
- **Per-feed refresh interval had no UI** — `Feed.fetch_interval` was stored but unreachable; `update_feed`'s whitelist already accepted it, no backend change needed. Added a ⏱ icon per feed row (both render paths — ungrouped `_feedItemHtml()` and the inline grouped-feed block in `_renderGroupTreeNode()`), `prompt()`-based edit mirroring the existing `_promptRenameGroup` pattern, `PUT`s `{fetch_interval: n}`. *(fixed: 2026-07-20)*
- **RSS feeds never auto-refreshed** — added `action_refresh_due_feeds` (`src/builtin_actions.py`, modeled on `action_summarize_emails`), registered in `BUILTIN_ACTIONS`/`BUILTIN_ACTION_INFO`/`HOUSEKEEPING_DEFAULTS` (`*/15 * * * *` cron, `ship_paused: True` — off by default, matches every other cron housekeeping task). Reuses the existing `_refresh_single(feed_id, owner)` helper (`routes/feed_routes.py:716`, already used by `/refresh-all`) rather than duplicating fetch/dedupe logic — each call wrapped in `asyncio.to_thread` since it's blocking network I/O and would otherwise stall the whole event loop, not just other scheduled tasks (same rationale as `_run_subprocess`'s existing to_thread usage). `TaskScheduler.ensure_defaults(owner)` runs per-owner already, so the action just filters `Feed.owner == owner` — no manual owner iteration needed. `TaskNoop` extends `BaseException` directly (not `Exception`), so raising it inside a `try/except Exception` block (matching `action_summarize_emails`'s exact shape) correctly propagates without a special re-raise guard. Verified live: directly invoked the action, confirmed all 12 feeds' `last_fetched` timestamps advanced. *(added: 2026-07-20)*
- **Article list had no infinite scroll** — `_loadArticles()`/`_renderArticleList()` always fully replaced the list; added an `append` mode to both (`_loadArticles(append)` increments `_pageOffset` and concats into `_articles`; `_renderArticleList(newArticles, append)` uses `insertAdjacentHTML('beforeend', ...)` for just the new batch instead of `innerHTML =`). Switched article click handling from re-attaching a fresh listener to every `.rss-article-item` on every render (line-level, `_onArticleListClick`) to a single delegated listener wired once in `_wireEvents` — appending would otherwise pile up duplicate listeners on already-rendered items. Added a `{ passive: true }` scroll listener triggering `_loadArticles(true)` within ~300px of the bottom, guarded by a new `_articlesLoadingMore` flag (reset alongside all 4 existing `_pageOffset = 0` resets on filter/feed/group/search change). Verified live: scrolled through 3 full pages (50→100→150 articles) with zero duplicate IDs, delegation confirmed working on newly-appended rows, and a filter switch correctly reset to a single fresh page. *(fixed: 2026-07-20)*
- **Fixed "can't drop a feed onto a collapsed group" from the earlier drag-and-drop batch** — root cause: `dragSort.js` assumes a stable DOM for one drag gesture (captures `draggedEl`/`placeholder`/`items` at drag-start, only mutates via its own internal `insertBefore`); a collapsed group has no `.rss-feed-item` rows to position against, and re-rendering (to expand it) mid-drag would orphan dragSort's captured references — `e.target` is fixed at dispatch time regardless of mid-dispatch DOM changes, so this can't be worked around inside the same event either. Fixed by intercepting in the CAPTURE phase, before dragSort's own bubble-phase `mousedown` listener on `#rss-feed-list` runs at all: if any group is collapsed, `stopImmediatePropagation()` (required, not `stopPropagation()` — both listeners are on the same element, and only `stopImmediatePropagation` stops sibling listeners on that same element/phase), expand everything, re-render. No changes to the shared `dragSort.js` module. Effect: first press on a handle while anything is collapsed just expands (no drag starts that click); a second press-drag then works normally, including landing in the previously-collapsed group. This exact capture + `stopImmediatePropagation` pattern already exists elsewhere in this codebase (`windowDrag.js:300`, `colorPicker.js`, `emojiPicker.js`, `notes.js`, `document.js`) — not a novel technique. Guarded with a `dataset.collapseDragWired` one-time flag on `#rss-feed-list` (mirroring `_wireRssWindow`'s `windowDragWired` pattern) since the listener is added inside `_renderFeedList()`, which runs on every re-render, unlike `dragSortModule.enable()` which has its own built-in re-registration cleanup a plain `addEventListener` doesn't. Verified live with a real simulated drag while a group was collapsed: first press expanded without moving anything, second press-drag correctly moved the feed into the group and persisted to the DB, then fully restored to a pre-test baseline (diffed identical). *(fixed: 2026-07-20)*
- **CSP `img-src` scoping investigated and closed as not implementable** — the roadmap TODO suggested scoping `img-src` to known CDNs (e.g. `*.ytimg.com`). Rejected: RSS aggregates arbitrary user-subscribed feeds, each hosting thumbnails/favicons on its own arbitrary domain (confirmed example: blackhillsinfosec.com serving its own podcast artwork) — a fixed allowlist would break legitimate images for any feed not on it. Documented the rationale as a comment at both CSP definition sites (`core/middleware.py:94` and `:121`, the report-only and enforced policy branches) so it isn't re-raised without context. *(closed, no code change: 2026-07-20)*
- **Sidebar felt scattered — no grouping between AI tools, personal/life tools, and appearance** — split the single `#tools-section` into `#ai-tools-section` (Deep Research, Compare, Brain, Library, Gallery, Cookbook), `#personal-section` (Email, Calendar, Notes, Tasks, RSS Feeds), and `#appearance-section` (Theme); reordered the icon-rail to match with `.rail-separator` dividers between groups. `UI_VIS_MAP` (`static/app.js`) updated from one `tools-section` key to three. Also fixed section-header titles being visually identical to the `.list-item` rows under them (same font-size/weight/color) — bumped to bold + uppercase + 0.04em letter-spacing (reusing `.theme-adv-group-label`'s exact value), so category headers read as headers when scanning the sidebar. Committed as `b205f82`, bundled with the run-script/systemd-service work below. *(fixed: 2026-07-24)*
- **No convenience way to run Odysseus on a stable, fixed port for a systemd service** — added `scripts/run-odysseus.sh` (fixed port 24950, sets `APP_PORT` so `internal_api_base()` resolves correctly) and fixed `install-service.sh`'s missing executable bit (`chmod +x` — pre-existing repo issue, unrelated). `odysseus-ui.service` is a checked-in template with a `YOURUSER` placeholder; do not fill it with a real username/port and commit — fill it locally to test, then revert before any commit (see [[roadmap]] for the currently-pending revert). *(added: 2026-07-24)*
- **Personal section's nested Email/Calendar/Notes/Tasks/RSS Feeds read as peer categories, not children of "Personal"** — same problem as the sidebar-title-contrast fix above, but inverted: Email is a nested `.section` with its own `.section-header-flex`, so once section titles got the bold/uppercase treatment, "Email" started competing visually with "Personal"/"Appearance" for attention as if it were a sibling category. Fixed by wrapping Email/Calendar/Notes/Tasks/RSS Feeds in a new `.section-nested-group` div (indented, with a left guide-line reusing `var(--border)`) and de-emphasizing just the nested Email title back to plain title-case (matching a `.list-item` label). Verified the Personal section still collapses/expands as a single unit — `section-management.js`'s collapse selector targets direct children generically, so the new wrapper div doesn't break it. *(fixed: 2026-07-24)*
- **Email 3-pane redesign: mobile view rendered completely blank** — the mobile `@media (max-width:768px)` CSS to collapse the 3 panes to one visible at a time (`.email-mobile-show-folders/-list/-reader` classes on `.email-3pane`) existed, but nothing in JS ever added those classes — a gap flagged during planning but not implemented before initial verification. Added `_setMobilePane()` plus "‹ Folders"/"‹ Inbox" back buttons wired at every folder-click/row-click/deep-link call site. Also fixed a related bug: re-tapping the *already-active* folder in the sidebar hit an early-return guard (`if (folder === state._libFolder) return`) that skipped the mobile pane-switch too, stranding mobile users on the folder pane with no way back to the list. *(fixed: 2026-07-24)*
- **Email 3-pane: folder-sidebar collapse could get stuck open** — the collapse toggle (`_toggleFoldersCollapsed()`) adds an `.email-folders-collapsed` class whose CSS forces `.email-pane-folders` to `width:0`, but the drag-resize handles (below) can leave a saved custom width as an *inline* `style="width:...px"` — inline styles always beat a plain (non-`!important`) class rule, so collapse silently did nothing if a custom width was ever set. Fixed by adding `!important` to the collapsed-state width rule. *(fixed: 2026-07-24)*
- **Email 3-pane: toolbar row sat flush against the pane edges, no breathing room** — `.email-pane-list` had zero padding (unlike `.email-pane-reader`'s `14px 18px`) because the old layout relied on the shared `.admin-card` wrapper's padding, which only insets the *outer* edges of the whole 3-pane row, not each individual pane. Added `padding: 6px 10px 0`. On mobile this same padding pushed the Select/refresh buttons off-screen at 390px width (regression, caught immediately via live testing) — scoped down to `6px 4px 0` inside the mobile media query. *(fixed: 2026-07-24)*
- **`odysseus-ui.service`'s real username/path had already been committed and pushed before it could be reverted** — the earlier promise to restore the `YOURUSER` placeholder before any commit didn't happen in time: `b205f82` (the sidebar-restructuring commit) carried the filled-in real values (`User=frieso`, `/home/frieso/odysseus`) straight through to `origin/dev`, discovered only when the user later asked to push again. Reverted to the placeholder in a follow-up commit, keeping the legitimate non-PII improvements from that commit (port 7000→24950, host `0.0.0.0`→`127.0.0.1`, added `Environment=APP_PORT=...`) — only the username/path went back to `YOURUSER`. **Caveat: this does not remove the real username from git history** — `b205f82` itself, already on the public remote, still has it in the diff/blame; fully scrubbing it would require a history rewrite (rebase + force-push), which was not requested and wasn't done. *(fixed: 2026-07-24)*
- **Email 3-pane: collapsing the folder sidebar hid the folder icons entirely, unlike the main app sidebar** — user pointed out that collapsing the main app sidebar keeps its icon-rail visible for navigation, but the Email folder-pane collapse (added the same day) just went to `width:0`, hiding everything. Changed the collapsed state to a 40px icon-only rail instead — folder icons, active-folder highlight, and a small unread-count dot all stay visible and clickable, with a hover tooltip (folder name + unread count, e.g. "Spam (6 unread)") added to each row for when labels are hidden. *(fixed: 2026-07-24)*
- **Theme → Customize tab: Neon Glow toggle rendered outside its card's border** — root cause was a flexbox overflow bug, not the glow animation itself: `.theme-fd-row` (Font & Layout section) packs 5 groups (Font, Density, Text size, Frosted, Neon glow) with no `flex-wrap`, and flex items don't shrink below their content's natural min-width by default — the two toggle-switch groups got pushed past the card's right edge instead of shrinking. Fixed with `flex-wrap: wrap` + `min-width: 0` on `.theme-fd-group`, and gave the toggle groups `flex: 0 0 auto` so they size to their own content instead of stretching when wrapped. *(fixed: 2026-07-24)*
- **Added a "new mail" pulse banner** — user asked for a banner when email arrives, beyond the existing sidebar breathing-dot indicator. `GET /api/email/unread-state` gained a `latest` field (subject/from_name/from_address of the newest message) — cheap, since `email_message_index` already caches this locally, no extra IMAP round-trip. `emailInbox.js`'s existing `_refreshUnreadCount()` (already polls every 60s and compares `max_uid` against a localStorage "last seen" threshold to drive the sidebar dot) now also fires a new top-right banner the first time a given `max_uid` is seen — guarded by a session-only `_lastAnnouncedMailUid` variable so it doesn't re-fire on every subsequent 60s poll while the same unread mail sits there. Skips entirely while the Email inbox is already open (its list/reader already reflect new mail live). Banner shows sender + subject, pulses 3 times over ~5.4s via a glowing box-shadow keyframe, auto-dismisses after 8s, has a close button, and clicking it opens the inbox. Verified the trigger logic, dedup, and real-content rendering via a console-log trace and direct DOM inspection — reliably catching the exact 8s-delayed / 8s-visible lifecycle via live screenshot proved impractical through the remote preview bridge (tool-call round-trip latency regularly swallowed the whole window between calls), but the underlying `setTimeout`/`clearTimeout` pairing was manually traced and is straightforward. *(added: 2026-07-24)*
- **Confirmed the systemd service is installed, enabled, and running** — `systemctl status odysseus-ui` shows `active (running)`, `enabled` (starts on boot), serving on port 24950 as configured. The earlier roadmap TODO ("user needs to run `./install-service.sh` themselves") is done. *(confirmed: 2026-07-25)*
- **Email folder-badge unread counts were unreadable — a generic rule was silently winning** — `.list-item span { color: var(--fg); font-size: 9.75px; }` (style.css:1916, specificity 0-1-1) beat the badge's own single-class `.email-folder-badge { color: var(--bg); font-size: 9px; }` rule (0-1-0), since the badge is itself a `<span>` inside a `.list-item`. On the navyseal theme that rendered the number in light `--fg` on top of `--red`'s own light cyan background — nearly invisible, described as "white". Fixed by scoping the selector to `.list-item .email-folder-badge` (0-2-0), no `!important` needed. *(fixed: 2026-07-26)*
- **Modals didn't respect the Theme → Customize → Font setting at all** — `.modal-content` (style.css:5438) hardcoded `font-family: 'Inter', -apple-system, ...` regardless of the user's choice (Monospace/Sans-serif/Serif/OpenDyslexic), while the rest of the app (`body`, style.css:105) correctly used `var(--font-family, 'Fira Code', monospace)`. This affected every modal in the app, not just Email — pre-existing, not introduced this session. Fixed by matching the same fallback chain; also dropped the Inter-tuned `letter-spacing: -0.015em`, which doesn't apply to the monospace default and body doesn't use it either. Verified live: switching to Sans-serif correctly re-flowed Email and Theme modal text with no layout breakage, then reverted to Monospace. *(fixed: 2026-07-26)*
- **Email reader action buttons: fixing text truncation didn't fix the real complaint** — first pass only bumped the narrow-reader-pane tile/label width by 4px so "Reply all" stopped clipping to "Reply a...", but the user's actual complaint was the whole 6-button cluster wrapping into a cramped 2-row grid of bordered icon+label tiles. Redesigned it properly: icon-only buttons (name reachable via the existing `title="..."` tooltip, same treatment the "More" button already had) fit all 6 in a single row, cutting the cluster's height from ~84px to ~37px. Recalculated the exact-fit pixel math for both narrow-reader-pane breakpoints (`@container emailreader max-width:450px` and `:380px`) consistently. *(fixed: 2026-07-26)*
- **Consolidated Reply/Reply All/Reply with AI into one dropdown off the Reply button** — user asked for this directly ("make the reply button have drop down (reply with ai)"), and it continues the same decluttering as the icon-only fix above — down to 4 top-level buttons (Reply▾, Forward, Summary, More) from up to 6. New `_showReplyMenu()` reuses the exact same `.email-card-dropdown`/`.dropdown-item-compact` styling and `bindMenuDismiss`/`_fitEmailDropdown` helpers as the existing "More" menu (`_showReaderMoreMenu`) rather than a new dropdown implementation. "Reply with AI" calls the pre-existing `_handleAiReplyButton()` unchanged (same cached-draft-first-click / Fast-Full-chooser-second-click behavior), just anchored to the Reply button instead of its own icon. A small CSS-triangle caret badge on the Reply icon signals it opens a menu. Scoped to the 3-pane reading pane only (`_buildEmailReaderHeaderHtml`/`_loadReadingPane`) — 3 other duplicated header blocks elsewhere in the file (open-as-tab, open-as-window, and one confirmed-dead `_toggleCardPreview`-era block) still have the old 3-separate-icons layout, left untouched pending a decision on whether to unify them too. *(fixed: 2026-07-26)*
- **Odysseus agent mode repeatedly retried a `sudo npm install` it could never complete, instead of stopping after the first failure** → found by reading the actual chat transcript from an in-app agent-mode session (`sessions` table id `35391cf2...`, titled "Installing Anthropic Claude Code", model `big-pickle`) after the user reported the agent behaving oddly. The user pasted terminal output showing `sudo npm install -g @anthropic-ai/claude-code` hit a blocked-postinstall-scripts warning on their own machine; across 3 separate assistant turns the agent's own bash tool tried to run that same `sudo` command itself (in its own sandbox, cwd'd to Odysseus's `data/` dir, not the user's home directory) — every attempt failed identically with `sudo: a terminal is required to read the password` (no TTY in the sandbox). The user called it out mid-conversation ("that's werid, I didn't paste anything you kept working with yourself"); the agent apologized and explained the mistake correctly — then repeated the exact same failing tool call in that same response, and again in the next turn. Root cause: `agent_loop.py`'s `TOOL_SECTIONS["bash"]` prompt already told the model about the sandbox's no-TTY limitation (`input()`, `curses`, `termios`, `pygame`, `tkinter` all fail, verify + tell the user to run it themselves) but never connected that same constraint to `sudo`/password prompts, and never told it to stop retrying an identically-failing command. Also confirmed: on this machine `npm config get prefix` is already `~/.npm-global` (user-owned), so `sudo` was never actually necessary for this install in the first place — plain `npm install -g` would have worked from the agent's own sandbox. Fixed by extending the exact same sentence in the bash tool's prompt section: `sudo`/password-prompt commands now explicitly named as another no-TTY failure mode, with instructions to check whether `sudo` is even needed (e.g. via `npm config get prefix`) and retry without it before assuming it's required, and to stop after one failed attempt rather than retrying identically. Full suite: 4639 passed, 3 skipped — this is a prompt-only change with no code path affected, so this doesn't verify the *model's* behavior actually changes, only that nothing broke. *(found and fixed: 2026-07-26)*
- **Agent mode could never run `sudo`, and `tool_progress` SSE events never reached the browser at all** → follow-up to the 2026-07-26 prompt-only fix, after the user pushed back that they wanted the agent to actually *succeed* at sudo ("it should run on my terminal system if i choose agent right?") rather than fall back to "paste this yourself". First correction: **agent mode already runs on the user's real machine as their real user** (subprocess as `frieso`, full filesystem access) — it is NOT an isolated sandbox, and the model's claim that it was is a confabulation, likely nudged by the prompt's "SANDBOX LIMITS" heading. Three real gaps were found and fixed:
  1. **No stdin.** `BashTool` passed stdout/stderr pipes but never stdin, so `sudo` had nowhere to read a password from. Now: the tool detects an unattended `sudo`, first probes `sudo -n true` (skips the whole prompt when a NOPASSWD rule or live ticket already covers it), otherwise blocks and asks the *browser* for the password via a new in-memory broker (`src/sudo_auth.py`), then rewrites `sudo` → `sudo -S -p ''` and feeds the password over stdin (one line per invocation, so chained `sudo a && sudo b` works). Password is cached per-owner for 15 min (mirroring sudo's own ticket lifetime), redacted from output defensively, and the cache is burned on a "try again" failure so a wrong password doesn't silently poison the rest of the turn.
  2. **`HOME` was force-overridden** to the agent workdir (`tool_execution.py`). This was the *deeper root cause of the original npm episode*: with `HOME` clobbered npm couldn't see `~/.npmrc`, resolved its global prefix to `/usr`, and therefore demanded sudo — whereas in the user's own terminal the prefix is `~/.npm-global` (writable, **no sudo needed at all**). The override bought no isolation either, since bash here is explicitly un-sandboxed and can read `$HOME` regardless; it only broke every config-driven tool (npm, git, ssh, gh). Now set to the real home; verified the agent again resolves `~/.npm-global` and reports "no sudo needed".
  3. **`tool_progress` was missing from the SSE relay whitelist** in `routes/chat_routes.py` (~line 1449). The agent loop emitted these events but the chat route silently dropped them, so they never reached the browser. This was a **pre-existing bug independent of sudo** — it's also why the "live elapsed time + tail of output" feature on a running bash card never displayed anything on this path. Adding `tool_progress` to the whitelist fixed both that and the new sudo prompt.
  Also plumbed `owner` through `_call_mcp_tool` → `_direct_fallback` (bash previously received `owner=None`, so the prompt/cache couldn't be scoped to a user). Frontend: new `static/js/sudoPrompt.js` modal (masked auto-focused input, shows the exact command, "remember for 15 min" toggle, Cancel/Esc, and a resume path that re-attaches to a still-pending prompt after a page reload) plus scoped CSS. New auth-gated endpoints `POST /api/agent/sudo/{password,cancel,forget}` and `GET /api/agent/sudo/status` — all verified to 401 unauthenticated, and `status` deliberately returns the request id + command but never the password. Bash tool prompt rewritten to say sudo IS supported and to stop claiming a sandbox. Verified end-to-end live in agent mode: modal appears mid-turn with the real command, Cancel unblocks the backend immediately and the tool returns a clean "not provided" message. Full suite green.

**Update, same day**: caused real user confusion before the success path was confirmed. An automated test (clicking Cancel programmatically to prove that path) was run in the same shared preview window the user was watching, leaving only the post-cancel "sudo prompt was cancelled or timed out" text visible with no modal to interact with — user reasonably read this as "still broken" and reacted accordingly (rightly frustrated that a commit had gone out with the fix apparently not working). Root-caused by checking `journalctl` against file mtimes (nothing in the systemd service's own log matched the timestamp — it was the shared dev-preview window, not the systemd instance, that had been touched). Lesson: once a fix reaches something the user can see live, stop scripting the happy/cancel path through it — surface the prompt and stop, so the user's own action is the actual proof, not a simulation of one. Second attempt: sent a fresh command, confirmed the modal appeared, and **did not touch it**. User typed their real password themselves and clicked Authorize — `sudo id -un` returned `root`, confirming the full stdin-injection path works with a real password end to end (which the stub-`sudo` test the same day could not prove). *(confirmed working with a real password: 2026-07-27)*

Also worth logging as a process note, independent of this feature: the systemd service (`odysseus-ui`, port 24950, the "always on" instance from the 2026-07-24 run-script/service work) has been running continuously since 2026-07-25 21:18 with no `--reload` — it has picked up **none** of the fixes from 2026-07-26 or 2026-07-27 (sudo, the tool_progress relay bug, the bash keyword hint, or anything else). It will need `sudo systemctl restart odysseus-ui` before any of this session's work is live there.
- **`sudo` still failed silently for wrapper scripts (`garuda-update`) that call sudo internally** → the user restarted the systemd service (confirmed via `systemctl show`/journalctl, new PID) and hit `garuda-update` on a fresh process with every fix loaded — it still asked for a password with no UI prompt. Root cause: `_mentions_sudo()` only detects the literal token `sudo` in the command text; `garuda-update` never contains that string, it's a separate script that shells out to `sudo` on its own. So the whole password-prompt path was never entered — the command ran raw over a pipe (no tty), and sudo immediately failed with `a terminal is required to read the password`. Fixed with a second-line defense in `src/agent_tools/subprocess_tools.py`: after any bash command fails, check the output for sudo's own tty/password-required error strings (`_looks_like_sudo_tty_failure`); if it matches, request a password through the same broker/modal as before, then **retry the identical command attached to a real pty** (`_run_via_pty` — `pty.openpty()` + `preexec_fn` doing `setsid()`+`ioctl(TIOCSCTTY)`, watching the pty output for a line containing "assword" and writing the password once). This makes the *internal* sudo call find a controlling terminal and prompt on it directly, which works regardless of how the wrapper script invokes sudo — no command-text rewriting needed for this path. Verified with a stub script that mimics sudo's tty-check (refuses without a tty, prompts+authenticates with one): correct password succeeds with the password redacted from output, wrong password fails and clears the cache, and an ordinary non-privileged command (`echo hello world`) is completely unaffected — confirms this fallback only activates on an actual tty-required failure. Full suite still green (4639 passed, 3 skipped, no new hangs from the pty path). *(fixed: 2026-07-27)*

**Update, same day — the pty fix wasn't enough for garuda-update specifically.** After a restart, the sudo password step worked (per the fix above), but the run then hung indefinitely at pacman's own `Proceed with installation? [Y/n]` prompt with zero UI indication of why. Live process inspection (`ps`, checking pty ownership via `ls -la /dev/pts/*`) found the actual cause: `garuda-update` re-execs itself under `sudo`, then again under `systemd-inhibit` — and one of those hops allocates a **second, separate** pty (confirmed: our own pty was `pts/2`, owned by the app's process; the pacman confirmation was happening on `pts/4`, owned by root, with no path from ours to it). Writing to the device node directly doesn't work either — a pty slave node write is delivered as *output* to the master reader, not as *input* the child's own `read()` calls would see, so a third party can't inject a keystroke that way regardless of permissions. Chasing that nesting to find and attach to the real pty was ruled out as unreliable and update-tool-specific. Instead, read `/usr/bin/garuda-update` and `/usr/lib/garuda/garuda-update/main-update` directly and found a real, documented flag: `--noconfirm` (parsed via `getopt` in `main-update`, forwarded to the internal `auto-pacman` expect script as `AUTOPACMAN_PACMAN_NOCONFIRM`). Unlike env-var approaches (an earlier `PACMAN_EXTRA_OPTS` guess turned out to be preserved through sudo's `--preserve-env` but never actually read anywhere in this version of the script), a CLI flag survives every re-exec hop via `"$@"` regardless of pty or env boundaries. Fix: `_add_noconfirm_to_garuda_update()` in `subprocess_tools.py` rewrites any `garuda-update` invocation to include `--noconfirm` before it ever runs, sidestepping the confirmation prompt entirely rather than trying to answer it. **This is a standing behavior change** — every future `garuda-update` call auto-confirms, including package conflict/replacement prompts, not just this one run — and was correctly blocked once by the permission classifier for exactly that reason (a prior "same thing" reply from the user wasn't explicit authorization for a persistent code change). Re-asked directly via `AskUserQuestion` and got an explicit yes before implementing. Full suite green (4639 passed, 3 skipped). *(fixed: 2026-07-27)*
- **Agent reported "I don't have a shell tool available" — the shell tool was genuinely never offered to it** → surfaced immediately after the sudo work above, when the user retried `run sudo id -un` and the model listed only `web_search`/`web_fetch`/`manage_memory`/`ask_user`/`update_plan`. Not a model hallucination this time: Odysseus picks a per-turn toolset, and `bash` was legitimately absent from it. Three facts combined: (1) `ALWAYS_AVAILABLE` (`src/tool_index.py`) is deliberately tiny — `{manage_memory, ask_user, update_plan}` — to keep trivial prompts from carrying every domain's schemas, so `bash` is never unconditionally present; (2) `bash` was therefore reachable only via ChromaDB semantic retrieval or the `files` domain map; (3) **ChromaDB is down** (`ToolIndex init failed … not reachable at localhost:8100`, visible in the server log), so retrieval fell through to the keyword fallback — and `_KEYWORD_HINTS` had **no entry mapping to `bash`/`python` at all**, logging `Keyword fallback selected: []`. Net effect: whenever ChromaDB is unavailable, the agent silently loses shell capability and correctly reports it has none. Fixed by adding a shell/command keyword hint → `{bash, python, manage_bg_jobs}`, kept deliberately concrete (`sudo`, `shell`, `terminal`, `run this command`, `install`, `npm `, `apt `, `systemctl`, `git `, `docker `, `chmod`, …) rather than bare verbs like "run", which would fire on "run a search"/"run research". Verified both directions: 8 shell phrasings (including the exact failing `run sudo id -un`) now select `bash`, while search/research/email/calendar/reminder phrasings still don't. Confirmed live — the same prompt that failed now reaches the sudo password modal. Worth noting the underlying degradation separately: with ChromaDB down, *semantic* tool retrieval is unavailable for every domain, and only keyword hints remain. *(fixed: 2026-07-27)*

**Update, next day — keyword hints were the wrong fix; `bash` is now unconditional.** The same "I don't have a bash/shell tool" failure kept recurring for a full day despite two rounds of keyword additions. The trigger that finally exposed the real problem: `"cool check"` — a context-dependent follow-up meaning "check if that thing finished", containing **zero** shell-ish words. No keyword list can ever catch that, and with ChromaDB still down there's no semantic fallback either (server log for that exact turn: `Keyword fallback selected: []`). Chasing individual phrasings was whack-a-mole by construction. Real fix: added `bash`, `python`, `manage_bg_jobs` to `ALWAYS_AVAILABLE` so tool *availability* is never decided by string matching — the model decides from conversation context whether to actually use them, same as `manage_memory`/`ask_user`. Cost is a fixed prompt-size increase on every agent turn, accepted as clearly worth it given a day of broken shell access. Safety unchanged: `NON_ADMIN_BLOCKED_TOOLS` in `src/tool_security.py` still gates these per-owner, so this only affects what admins are *offered*, not what non-admins can run. One deliberate carve-out preserved (`src/agent_loop.py`): when a **workspace folder is attached** and the message is low-signal, `bash`/`python` are explicitly subtracted back out so a vague "look at the project" surfaces read-only nav tools only — caught by `test_low_signal_with_workspace_surfaces_readonly_file_tools`, which failed on the first attempt and correctly flagged that the blanket change had trampled an intentional safety rule. Verified live end-to-end, not just in schema: with ChromaDB down and keyword fallback empty, `"cool check"` produced `tools_sent=8` including `bash`, the agent invoked bash, ran a real background job, and reported its output on the auto-continuation turn. *(fixed: 2026-07-28)*
- **pacman's ANSI colour codes leaked into the UI (and into the model's context) as raw escape garbage** → once the pty fallback landed, `garuda-update`'s progress display rendered in the tool-output panel as walls of `[m[0;37m[m[0;37mo[0;37m…`. Cause: a pty makes pacman believe it has a real terminal, so it switches on colour/cursor-movement codes and redraws progress bars in place with bare `\r` — but nothing downstream was interpreting or stripping them, so the raw bytes went both to the browser *and* into the tool output the model reads. Fixed with `_clean_pty_output()` in `subprocess_tools.py`: strips ANSI CSI/escape sequences and converts `\r` redraws into plain newlines, applied to both the streamed progress tail and the final output. Two details that matter: the tail is now recomputed from the whole accumulated buffer rather than per-chunk, since an escape sequence can straddle a 4096-byte read boundary and per-chunk stripping would leave fragments; and this only touches the pty path, so ordinary piped commands are unaffected. Verified through the real `BashTool` code path with a stub emitting pacman-style colour + `\r` progress: output came back clean with no `\x1b` bytes and the password still redacted. *(fixed: 2026-07-28)*
- **Real-time email push, notification sound, and a numbered unread badge (2026-08-02)** — user reported unread counts never update while the inbox is open (true: the only client poll explicitly skips itself while the library's open, on the assumption the open view "reflects new mail live," which nothing actually implemented), plus asked for a notification sound and a persistent icon the "new mail" pulse banner could collapse into. Brainstormed via the `superpowers:brainstorming` skill; scaled down to a lightweight inline design (no committed spec doc, no separate writing-plans step) to match this project's established norms — the user has been explicit all session about disliking process ceremony and about not committing without being asked.
  - **Backend**: new `src/email_notify.py` — a background loop (`_email_notify_loop`, wired into `routes/email_pollers.py`'s existing `ODYSSEUS_INPROCESS_POLLERS`-gated `_start_poller()`, not a bespoke `app.py` task, so it respects the same operator kill-switch as the other IMAP pollers) polls each enabled account's `STATUS INBOX (UIDNEXT)` every 10s — cheap, no full fetch — and on change publishes a content-free `{"type": "new_mail", ...}` nudge to an in-memory per-owner pub/sub broker (mirrors `src/agent_runs.py`'s subscriber-set pattern). New `GET /api/email/notify/stream` (`routes/email_notify_routes.py`) exposes it as SSE. The nudge carries no message data on purpose — the frontend just re-runs its *existing* fetch paths on receipt, so no email-parsing logic is duplicated between the poller and the real routes.
  - **Real bug found only through live verification**: `/api/email/notify/stream` wasn't in `app.py`'s `_TIMEOUT_EXEMPT_PREFIXES`, so `_RequestTimeoutMiddleware`'s 45s hard timeout would have killed the connection in production (SSE endpoints are long-lived by design and must be listed there — same requirement as `/api/chat`, `/api/shell/stream`). Caught by watching server logs during testing, not by reasoning about the code; added to the exempt list.
  - **Frontend**: `#email-unread-dot` (sidebar Email section header) is now a real numbered badge (`.has-count` class + textContent), not just a presence dot — verified live against the user's real Gmail account (correctly tracked a live-changing count, including 14 new messages arriving mid-session). The "new mail" pulse banner now collapses/shrinks toward the badge's measured screen position on dismiss (`--collapse-x`/`--collapse-y` CSS custom properties set from `getBoundingClientRect`) instead of fading in place. Sound is a synthesized two-tone chime via the Web Audio API — no audio asset to source/license — gated by a new `email_notification_sound_enabled` pref (default on) via the existing generic `/api/prefs/<key>` mechanism, with a toggle added to Settings → Email → Notifications; `memory.js`'s `syncPrefToggle` helper was exported so it could be reused rather than re-implemented.
  - **Follow-up gap, caught by the user from a screenshot**: the collapsed icon-only sidebar rail (`#icon-rail`, shown when the full sidebar is hidden) had no unread indicator at all — only the expanded section-header dot got the badge treatment. Fixed by mirroring `notes.js`'s existing `rail-notes-badge` pattern exactly: a small absolutely-positioned numbered badge (`.rail-email-badge`) appended to/removed from `#rail-email`, driven by the same `shouldShow`/`unreadCount` values already computed for the main dot. Verified live by forcing the rail visible (`sidebar.classList.add('hidden')` + `window.syncRailSide()`) and confirming the badge renders and is positioned correctly in the button's corner.
  - 7 new tests (`tests/test_email_notify.py`) covering pub/sub owner isolation, multi-subscriber fanout, cleanup-on-disconnect, and UIDNEXT parsing against a fake IMAP connection. Full suite green throughout (4646 passed, 3 skipped).
  - **Process notes worth keeping**: this session is also where the user caught two of my own mistakes worth remembering — (1) I kept committing and pushing after every incremental fix off one early "commit and push" approval, which left the repo history messy enough that it had to be squashed/cleaned up afterward (see the sudo/pty work above); stopped doing that, now ask before every commit. (2) I once claimed a fix was "verified" based on a schema-only check (bash present in the tools list) when the user's actual reported failure was behavioral (the agent not calling bash) — schema presence is necessary but not sufficient evidence; re-tested end-to-end (bash actually invoked, real output returned) after being called on it.

---

## Decisions

- **Vanilla JS frontend, no framework** — SPA uses raw ES modules with no build step. CSS is monolithic. Design choice to avoid framework churn.
- **SQLite single-file database** — Not designed for multi-server horizontal scaling. All data in `data/` directory.
- **Docker Compose for bundled services** — ChromaDB, SearXNG, ntfy run as sidecars. GPU overlay pattern via Compose files.
- **ChromaDB as external service** — Not embedded; connects via HTTP. Degrades gracefully if unavailable (503 vs crash).
- **Fernet encryption for stored secrets** — Email passwords, vault items encrypted at rest. Key at `data/.app_key`.
- **Agent loop runs in-process** — No separate agent worker. Uses asyncio for concurrency.
- **Built on opencode agent framework** — Agent tool system, MCP, and skill system derived from opencode.
- **Deep Research adapted from Alibaba Tongyi DeepResearch** — Multi-step research with visual report output.
- **Cookbook based on llmfit** — Hardware fit scoring for model selection.
- **Search providers use a pluggable registry** — `PROVIDER_REGISTRY` (per-provider metadata via the `ProviderInfo` dataclass: label, needs_key, needs_url, key_setting, env_var, hint, has_additional) + `PROVIDER_FUNCTIONS` (name→search fn) in `services/search/providers.py`. `_call_provider` does a registry lookup instead of if/elif. Adding a provider = add a `ProviderInfo` entry + a search fn + register it in `PROVIDER_FUNCTIONS`. `GET /api/search/providers` serves this metadata so the settings UI renders key/URL fields and hints dynamically (no hardcoded provider JS). `PROVIDER_INFO` retained as a backward-compatible `(label, needs_key, needs_url)` tuple map. *(2026-06-08, commit 182bd15)*
- **Deep research is a web-search pipeline, not a single LLM call** — `src/deep_research.py` runs: classify → plan (3–6 sub-questions) → loop[generate queries → search → scrape/extract (dropping low-quality) → synthesize (compresses/dedupes) → stop?] → final report via the fixed `FINAL_REPORT_PROMPT`, capped by `research_max_tokens`. It does **not** honor a user-supplied output template, and its web-grounding adds little for conceptual/knowledge-synthesis tasks. For structured, no-compression documents, chat/agent mode (with the `web_search` tool) is the right tool, not deep research. Fixed 2026-06-12 to at least honor a user-supplied template when detected — see the Fixed section above.
- **Search runs one provider at a time with silent fallback** — `_build_provider_chain(primary)` = `[primary] + (search_fallback_chain or ["duckduckgo"])`. A provider with no API key returns `[]` (not an exception), so the chain silently advances. The provider that actually returned results is recorded in `DeepResearcher.providers_used` → surfaced in the report stats `Search:` field, and logged as `Research search: <prov> returned N results`. Odysseus does NOT query multiple providers in parallel and merge.
- **PDF support is read + form-fill + browser-print only** — Odysseus reads/extracts PDFs (pypdf, PyMuPDF/fitz) and fills+exports PDF *forms* (`/api/document/{id}/export-pdf`, requires a linked source PDF). Research reports export to PDF via the browser print dialog (`window.print()`, `src/visual_report.py:897`). There is **no** server-side text/markdown/HTML→PDF generation engine (no weasyprint/reportlab/pdfkit/fpdf), and **no** agent tool to emit a `.pdf` from generated content.
- **Skills are file-based SKILL.md, not a DB table** — under `data/skills/<category>/<name>/SKILL.md` (YAML frontmatter + body), managed by `SkillsManager` (`services/memory/skills.py`), created via `add_skill(...)` / `POST /api/skills/add`. Owner-scoped via the `owner` frontmatter field; published skills (`status: published`) always qualify for prompt injection (drafts gated by `skill_autosave_min_confidence`, max `skill_max_injected` per request). The Skills tab "Built-in" section is separate — it lists the agent's native tools from `agent_loop.TOOL_SECTIONS`, not editable skills. *(correction 2026-06-09: earlier notes incorrectly assumed a `skill_definitions` DB table.)*
- **Memory is on by default** (`memory: True` in `DEFAULT_SETTINGS`) — backed by the `memories` table + `data/memory.json`, with a vector copy in `data/memory_vectors/` (ChromaDB/fastembed). Empty until conversations populate it; it is active, not broken, when the list looks empty on a fresh install.
- **Kimi (Moonshot) is a first-class provider** — OpenAI-compatible, base `https://api.moonshot.ai/v1` (`.cn` also recognized). Wired in `static/js/slashCommands.js` (setup `kimi`/`moonshot`), `static/js/providers.js` (`_ENDPOINT_LABELS`), and `src/llm_core.py` (`_provider_label`). Uses the generic `openai` provider path (no special headers); logo already existed in `providers.js`. Same pattern as DeepSeek/Mistral (display-only label, not a behavior-distinct provider id). Registered in all four provider lists: `slashCommands.js`, `providers.js`, `llm_core.py`, and the `#adm-epProvider` `<option>` in `static/index.html` — missing the 4th list caused the Settings dropdown not to show it (fixed 2026-06-19).
- **RSS feed reader uses a Notes-like centered modal** — consistent with Notes, Email, Calendar behavior (centered backdrop modal with drag/snap/dock via `makeWindowDraggable`)
- **RSS reader uses feedparser directly** — rather than httpx+XML parsing. Battle-tested, covers RSS and Atom both.
- **`services/feed/full_content.py` uses trafilatura** — lightweight, no browser engine needed, produces clean Markdown
- **`_refresh_single()` runs as background task** via `BackgroundTasks` — non-blocking refresh-all with per-feed progress tracking
- **AI summary reuses `src.llm_core.query_llm()`** — no new provider infra
- **No OPML validation on import** — best-effort parse, errors silently drop malformed outlines
- **CSP `img-src` was missing `https:`** — blocked YouTube thumbnails (from `i*.ytimg.com`) and favicons. Fixed by adding `https:` to the allowlist.
- **YouTube embed → Error 153** — YouTube's in-page embed (iframe) fails with Error 153 ("Video player configuration error") for some videos. Embed URL returns HTTP 200 but empty content. Caused by YouTube-side restrictions (embedding disabled, shorts, etc.) not CSP. Three approaches attempted: (1) inline iframe in reader → Error 153; (2) `window.open` popup → opened as new tab (browser ignores popup features); (3) in-page modal overlay with embed + "Watch on YouTube" fallback link → embed still Error 153, fallback link opens video directly on YouTube (counts views). Current approach: in-page modal tries embed on open, shows fallback link below.
- **RSS nested groups use self-referential FK (`FeedGroup.parent_id`)** — unlimited depth, flat `_groups` array with `_buildGroupTree()` converting to tree client-side. Collapse/expand state (`_groupCollapsed` Set) is ephemeral (not persisted) — reset on page reload. *(2026-06-13)*
- **OPML group hierarchy stored as `group_path` list** — `parse_opml` returns list of ancestor `<outline>` titles from root to leaf; `import_opml` walks this path, creates/missing `FeedGroup` records with correct `parent_id` chain, then sets `Feed.group_id` to the leaf group. *(2026-06-13)*
- **Batch move/delete uses per-feed API calls** — no new batch endpoints; batch move calls `PUT /api/feeds/{id}` in a loop, batch delete calls `DELETE /api/feeds/{id}` in a loop. `.rss-feed-checkbox` class on each feed item; `.rss-batch-bar` shown conditionally when `_selectedFeedIds.size > 0`. *(2026-06-13)*
- **Selections cleared on `_loadFeeds()`** — prevents stale references after group changes, creates, or deletes. *(2026-06-13)*
- **Service worker cache version bump is the only reliable way to force re-fetch** — static files are aggressively cached; `sw.js` version increment needed. Bumped from v328 to v334 across sessions. *(2026-06-13)*
- **`query_llm()` is the one-shot LLM helper for non-streaming background tasks** — resolves model via `resolve_task_endpoint()` (task → utility → default chat model chain), calls `llm_call()`. Both RSS summary endpoints import it lazily from `src.llm_core`. It did not exist before 2026-06-19 — adding it fixed both AI-summary buttons. *(2026-06-19)*
- **RSS group summary uses a reduced prompt to avoid 45 s REQUEST_HARD_TIMEOUT** — limited to 10 unread articles × 400 chars each, max 6000 chars total prompt, `max_tokens=600`. The 45 s timeout is set in `app.py` via `REQUEST_HARD_TIMEOUT` middleware. Local models (llama3.1) on long prompts reliably hit this. *(2026-06-19)*
- **Kimi is registered in all four provider lists** — see Kimi decision above. *(fixed 2026-06-19)*
- **RSS select-mode button lives in the Groups header, not the filter bar** — moved so the filter bar stays visually clean. Renders as icon+label button (`<svg>` + `<span>Select</span>`). *(2026-06-19)*
- **RSS right pane auto-loads on open** — changed from conditional `if (_activeFeedId) _loadArticles()` to unconditional; "All Feeds" loads immediately so the pane is never empty when the reader opens. *(2026-06-19)*
- **Navyseal theme does not define `--accent-primary`** — only bg/fg/panel/border/red. All rules that used `var(--accent-primary)` with no fallback rendered transparent in Navyseal. Fixed with CSS fallback `var(--accent-primary, var(--fg))`. *(2026-06-19)*
- **Merge conflict resolution strategy (2026-06-25)** — upstream's `services/search/providers.py` had reverted to a 6-provider simple tuple dict; kept our full `ProviderInfo` dataclass + 10-provider `PROVIDER_REGISTRY`. For `settings.js` fallback chain UI: upstream's row-based select approach was kept over our chip-based approach because the post-conflict code referenced upstream's `sel`/`row` variables. Research panel: removed duplicate `research-no-past-hint` div, adopted upstream's `_pickResearchHint()` textarea placeholder, kept our category buttons row. Index.html: kept our research settings inputs + avatar upload file input over upstream's stripped-down version.
- **`settings.js` fallback chain uses row-based selects (upstream), not chip/drag UI (ours)** — upstream's approach was chosen because the shared post-conflict code (`sel.addEventListener`, `row.appendChild`) was written for it. The drag-reorder chip UX was discarded in the merge. *(2026-06-25)*
- **Second major upstream merge (2026-07-19)** — merged `upstream/dev` (301 commits) into local `dev` via `git merge upstream/dev`. Two real conflicts: `static/sw.js` (both sides bumped the cache version independently, v334 local vs v344 upstream — resolved by taking v345, one past both, to force a clean cache reset for everyone) and `tests/test_kimi_code_user_agent.py` (modify/delete: we'd locally deleted this file along with 4 other Kimi/LM-Studio test files as an uncommitted, unexplained local change; upstream had since expanded it with new coverage). Resolved by restoring upstream's version — the other 4 deleted test files (`test_kimi_code_hosts.py`, `test_lmstudio_discovery.py`, `test_lmstudio_models_url.py`, `test_lmstudio_vision.py`) were untouched by upstream since the merge-base, so git correctly kept them deleted (standard modify/delete 3-way merge semantics, not a decision that needed overriding).
- **Post-merge test/lint fixes (2026-07-19)** — full suite (4632 tests) had 3 failures after the merge, both self-inflicted by carrying local-only code across the merge boundary, not upstream bugs: (1) `services/search/providers.py`'s Bing provider (local-only, not present upstream) still had an inline `"User-Agent": "Mozilla/5.0"` instead of the shared `WEB_FETCH_USER_AGENT` constant that the rest of the file (and upstream's version) already used — a repo-wide test (`test_web_user_agent_constant.py`) catches this pattern; fixed by using the constant. (2) `tests/test_deep_research_synthesis_resilience.py`'s `_researcher()` helper builds a `DeepResearcher` via bare `__new__` (skipping `__init__`) and only sets `synthesis_window`/`max_report_tokens`; our 2026-06-12 fix added a `self.research_plan` read in `_synthesize`, which the stub never got — fixed by adding `r.research_plan = ""` to the test helper. After both fixes: 4632 passed, 3 skipped, 0 failed.
- **Upstream split the tool/route monoliths into domain packages** (merged 2026-07-19) — `src/tool_implementations.py` (205KB) is now a ~5KB facade re-exporting from `src/tools/` (system/cookbook/calendar/contacts/image/notes/research/search/vault); admin agent tools moved to `src/agent_tools/`; `contacts`, `gallery`, `history`, `memory`, `research` routes moved from flat `routes/<domain>_routes.py` files into `routes/<domain>/<domain>_routes.py` subpackages, with the old flat path kept as a thin re-export shim. New `src/model_capability_readers/` package (per-provider model capability schema, upstream commit `4f04c34`, #2739). New `src/search/` package appeared alongside the existing `services/search/` — not yet confirmed whether it's wired up or upstream work-in-progress; see [[roadmap]]. Full details in [[structure]].
- **Data-loss incident during RSS group-delete testing (2026-07-20), cause not conclusively identified** — while live-testing the newly-added group-delete button against the running dev server, 3 of the user's real groups ("Front-End", "Linux", "Tech") disappeared from the DB. The test itself was scoped to a single throwaway group (`__delete_test_group__`) created and deleted via a targeted DOM query matching that group's exact name; code review of `delete_group` (`routes/feed_routes.py`) confirms it only reparents/ungroups the *specific* group_id in the DELETE URL, no cascading logic that could explain deleting unrelated groups. No access logs survived the multiple `preview_stop`/`preview_start` cycles from that session to forensically confirm the request history. Impact was limited — verified via direct DB query that total feed count (12) exactly matched the two surviving groups' feed counts, with zero orphaned/ungrouped feeds, meaning the 3 lost groups held no feeds; only empty organizational labels were lost, no articles/read-state/subscriptions. User declined an offer to auto-recreate them. **Caution for future sessions**: prefer testing destructial-adjacent UI (delete buttons, bulk operations) against synthetic/throwaway data with a captured pre-test baseline (`SELECT` snapshot to a file) that can be diffed/restored, and avoid rapid repeated `preview_stop`/`preview_start` cycles during a session involving mutations, since it forecloses forensic log recovery if something unexpected happens. The later drag-and-drop feature test (same session) followed this stricter protocol — full baseline snapshot before, diffed identical after restore — and is the model to follow going forward.
- **Email redesigned from an expanding-card modal to a persistent 3-pane webmail layout (2026-07-24)** — user asked to make Email look like a real 3-pane mail client (folder sidebar / message list / reading pane) after sharing a reference screenshot; explicitly chose the full-rewrite option over a lighter restyle. Layout-only scope, all existing behavior preserved. Key structural points: `IMAP4.status(mailbox, '(UNSEEN)')` is the correct call for a per-folder unread *count* (not `SELECT`/`EXAMINE`, which only expose `EXISTS`) — added to `list_folders` (`routes/email_routes.py`) as a sibling `unread_counts` dict alongside the existing `folders` array, so old callers treating `folders` as `string[]` don't break. `_createCard()`/`_toggleCardPreview()` (old expanding-card renderer) were split into pure-data (`_emailRowData`) + new DOM builders (`_createEmailRow`, `_loadReadingPane`) rather than rewritten in place, reusing the exact same stale-response-guard pattern, attachment/inline-image wiring, and reply/AI-reply/translate/tag logic verbatim. Folder sidebar reuses `.list-item`/`.section-header-flex` (same classes as the main app sidebar) and `sortedFolders()`/`folderDisplayName()` (from `emailInbox.js`, not duplicated). Old `.doclib-card`-based code (`_createCard`, `_toggleCardPreview`, `_findSiblingEmailCard`, matching CSS) deliberately left in place but unreferenced — shared CSS rules with Documents/Memory panels make surgical removal riskier than the payoff right now; flagged as a follow-up cleanup, not done yet. Verified live against a real Gmail account (89 messages, correct unread counts per folder) across every preserved feature (folder switch, search, filter, bulk-select, compose, reply, the "More" menu, Scheduled folder) plus the full backend suite (4639 passed). Not yet committed — see [[roadmap]].
- **Email 3-pane: folder sidebar is collapsible and drag-resizable (2026-07-24)** — added alongside the redesign after a follow-up request for more layout control. Collapse toggles a class that forces `width:0 !important` on the folder pane (see the Fixed entry above for why `!important` is needed against the drag-resize inline style). Resize handles are thin flex items between panes; drag math is tracked in a closure variable (`currentSize`) rather than re-measured from the DOM in the pointerup handler, to avoid depending on a layout flush having settled by the time the handler runs. Both collapse state and pane widths persist per-browser via `localStorage` (`odysseus.email.foldersCollapsed`/`.foldersW`/`.listW`), restored on next `openEmailLibrary()` call. Desktop-only — hidden via the existing mobile media query.
- **Third major upstream merge (2026-08-04) — 1957-commit divergence, 117 conflicted files, fully resolved and committed (`ca9fee69`)** — user asked to sync after noticing the `upstream` remote pointed at a stale pre-rename URL (fixed earlier the same session). Given the enormous scope, the user's standing instruction was "review conflicts together," refined mid-merge via AskUserQuestion to "flag only real decisions, batch the clear ones" — i.e. don't ask for confirmation on files that are a clean upstream superset with zero local content at risk, but do stop and ask before discarding anything non-trivial that's local-only. This was enforced by a permission classifier that blocked several bulk "take upstream" actions until explicit confirmation was obtained, even when reasoning seemed sound in the same turn — text explanation alone wasn't accepted as consent for judgment calls.
  - **Local-only features confirmed preserved**: RSS Feed Reader (~1000 lines of CSS alone, `core/database.py` models, `core/middleware.py` CSP rules), the email 3-pane layout and its `email-row`/`email-pane-*` DOM structure (vs. upstream's older card-grid), sudo password-prompt broker and pty fallback, real-time email SSE push + notification sound + pulse banner, avatar upload, Kimi/Moonshot provider detection, 4 extra web-search providers (Bing/Search1API/Firecrawl/Exa) via a `ProviderInfo` dataclass registry, Deep Research's 2026-06-12 prompt-engineering fixes, the sidebar's nested-group structural redesign, and a deliberately-disabled "from-sender thread search" feature (kept in code, hidden via CSS `display:none !important` + JS `.remove()`, per an inline comment saying the UX was "too buggy to ship").
  - **Real upstream improvements taken wholesale**: `apply_patch`/`todowrite` agent tools, persistent tmux-backed shell sessions (kept side-by-side with sudo, not merged into it — sudo needs a broker round-trip that tmux's poll-for-marker loop can't service), the `_anthropic_rejects_temperature` regex fix for major-only model IDs like `claude-opus-5` (issue #5753, directly relevant — that's the model this session ran on), incognito mode moving from "in-memory but persisted-by-a-later-turn" to a fully isolated ephemeral transcript store (real privacy fix), OAuth mailbox-identity verification (rejects a token if it doesn't match the account's configured IMAP/SMTP login), per-account email writing style and multi-account `account_id` threading through AI-reply endpoints, the model picker's hide-list → explicit pin-list refactor for API providers (fixes the same model ID appearing checked on both OpenRouter and OpenAI), and the full MLX/mflux/diffusers image-serving stack (backend detection, dependency installers, Cookbook UI) — which is also why local's own "Image tab removed — text→image gen is gone from this build" 2-option dropdown was reverted back to 3 options (Standard/Vision/Image): the original blocker (no real backend support) no longer applies now that this exact merge adds it.
  - **Corruption incidents, both self-caught via mandatory marker-balance checks, not user-reported**: an over-greedy Python extraction script in `static/index.html` swept a nested unrelated hunk into an "ours" slice, leaving unbalanced markers (8 open vs. 7 close each) — recovered via `git checkout -m -- static/index.html` to regenerate clean markers, then redid every edit with per-edit balance verification going forward. Separately, `static/app.js`, `static/js/document.js`, and `static/js/sessions.js` each had one duplicate-content merge artifact (the same function or block appearing twice, once broken/orphaned) — not marker corruption, but git's 3-way algorithm matching stale local text against a new upstream function positioned nearby; all found via `grep -c` symbol-uniqueness checks and Node's `--check`/TypeScript diagnostics catching "Cannot redeclare" or reference errors before staging.
  - **Verification**: every changed `.py` file passed `py_compile`, every changed `.js` file passed `node --check`, `static/style.css` was checked for balanced braces (found and fixed one real 1-brace deficit from a Python-script line-boundary slice error in the RSS section), and the full backend suite ran clean at the end — 4796 passed, 3 skipped, 0 failed. `app.py` imports and initializes without error (ChromaDB warnings are pre-existing/expected, unrelated to the merge — that service isn't running locally).
  - **Process note**: for large hybrid-merge files, sample-then-decide beat resolve-blind — extracting full hunk context (both sides plus surrounding unconflicted code) before choosing consistently surfaced real local-only content that a simple "which side is longer" heuristic would have discarded (e.g. `_swapReaderToUid`, `_detachActiveEmailForBackground`, the `_inferBaseRepo` gated-repo fallback that had been silently dropped by an earlier non-conflicting auto-merge and needed manual restoration).
- **OpenCode Zen (and any other pre-existing API endpoint) shows zero models until pinned — not a bug (2026-08-05)** — after the upstream merge, user noticed OpenCode Zen "doesn't provide models" in the picker. Traced to the pin-list refactor in `routes/model_routes.py` (`_picker_models_for_endpoint`): API-kind endpoints (anything not local/Tailscale) now require explicit `pinned_models` before showing in the picker; the migration path (`_legacy_visible_api_models`) only auto-carries over the old hide-list state when `hidden_models` was previously set — an endpoint that was never touched under the old system (like this one, cached_models present, hidden/pinned both empty) intentionally shows `models: []` with `model_count` reporting the real inventory separately. Confirmed intentional, not a regression, via an existing upstream test (`test_list_api_endpoint_reports_inventory_count_when_none_pinned`) that locks in exactly this behavior — a same-shaped "fix" was tried and reverted once this was found. Fix is user-side: pin the wanted models for the endpoint in Settings → Added Models.
- **docker-publish CI regression from the merge, found and fixed (2026-08-05)** — every push to `dev`/`main` since the 1957-commit sync had been failing image builds: `ERROR: failed to parse ref "ghcr.io/YahyaZekry/odysseus": invalid reference format: repository name (YahyaZekry/odysseus) must be lowercase`. Root cause: the merge took upstream's `docker-publish.yml` wholesale as a "pure CI improvement," which silently dropped a local-only fix (`da282e61`, 2026-07-19) — a `prep` job that lowercases `github.repository` before using it as the GHCR image name, needed because `YahyaZekry/odysseus` has mixed case and upstream's own repo name never did. Restored the `prep` job, rewired onto upstream's current job structure (`build`/`merge` now `needs: prep` and read `needs.prep.outputs.image_name`). Verified live: the next push built and pushed successfully end-to-end.
- **Fork lineage: GitHub's fork-parent (`arcahyadi/odysseus`) is NOT the actively-tracked project (2026-08-05)** — user was confused why GitHub's compare UI showed "X commits ahead of arcahyadi/odysseus:main" instead of odysseus-dev/odysseus. Confirmed via `gh api repos/YahyaZekry/odysseus` that this fork's official GitHub fork-network parent is `arcahyadi/odysseus` (stale, last pushed 2026-06-23, 42 stars, itself not a fork), a completely different repo from `odysseus-dev/odysseus` (the 84.7k-star actively-maintained project the `upstream` git remote correctly points to, last pushed same day). The two share only early common history (merge-base back near project inception) — GitHub's fork-graph tracking is independent of git remotes and can't be changed without a fresh fork, which isn't worth the disruption. The `upstream` remote + manual/scripted merge workflow (not GitHub's "Sync fork" button, which would target the wrong repo) is the correct and only way to pull real updates.
- **Added `.github/workflows/upstream-sync.yml` — daily cron + manual dispatch, auto-PR (2026-08-05)** — diffs `dev` against `odysseus-dev/odysseus:dev`, and if there's anything new, (re)creates a persistent `upstream-sync` branch with the merge already attempted: clean merges open a normal PR ready to review, conflicted ones commit the conflict markers as-is and open a **draft** PR so resolution is a plain checkout-and-fix task instead of restarting from scratch. Discovered live that GitHub blocks Actions-created PRs via the default `GITHUB_TOKEN` even with the repo's "Allow GitHub Actions to create and approve pull requests" setting enabled (confirmed via API, `can_approve_pull_request_reviews: true`) — three identical `Resource not accessible by integration (createPullRequest)` failures after enabling it, including after a propagation-delay retry. Workflow now uses a `SYNC_PR_TOKEN` fine-grained PAT secret (falls back to the default token, which will keep hitting the same error until the secret is added) — **user still needs to create the PAT and add the repo secret**, tracked in [[roadmap]].
- **`upstream-sync.yml` fully working (2026-08-05)** — the `SYNC_PR_TOKEN` fine-grained PAT was correctly scoped from the start (`Contents` + `Pull requests` read/write on `YahyaZekry/odysseus` specifically), so the earlier "Resource not accessible by personal access token" failure wasn't a token-scoping problem. Root cause: `gh pr create`/`gh pr edit` call GitHub's GraphQL `createPullRequest` mutation, and fine-grained PATs have a known gap there — the identical permissions work fine over REST. Switched the workflow's PR step to `gh api repos/.../pulls` (POST to create, PATCH to update) instead. First live run after the switch succeeded and opened [PR #1](https://github.com/YahyaZekry/odysseus/pull/1), "Sync with upstream (5 commit(s))", not a draft (clean merge). Roadmap item closed.
- **PR #2's 3 failing CI gates root-caused and fixed, not just merged around (2026-08-09)** — the 2026-08-05 log noted PR #1 merged despite 3 failing gates "since none are branch-protection-enforced," but left the actual cause uninvestigated. This session traced all three: (1) `pr-description-check.yml`'s two jobs skip bots via `github.event.pull_request.user.type != 'Bot'`, but the sync PR is authored through `SYNC_PR_TOKEN` (a PAT tied to a real user account, `YahyaZekry`), so `user.type` is `'User'` and the skip silently never fires — every future sync PR would keep failing the Conventional-Commits-title and description-template checks forever, since the workflow hard-codes a fixed, non-compliant title/body format (`upstream-sync.yml:107-112`) that can never satisfy them. Fixed by also excluding `github.head_ref == 'upstream-sync'` in both jobs' `if:` (commit `89e9c64`, pushed straight to `dev` — no branch protection existed yet at that point). (2) `dependency-review`'s "Dependency review is not supported on this repository" error is unrelated to the bot-skip — GitHub's Dependency graph was simply disabled repo-wide, failing the check for every PR, sync or not. Enabled via `PUT /repos/.../vulnerability-alerts`. Verified by editing PR #2 (retriggers `pull_request_target`'s `edited` type, which picks up the just-merged workflow fix from `dev`) and re-running the dependency-review job — all 3 gates went green, then merged (`c70a2b0`). (3) Also added a minimal branch-protection ruleset on `dev` (blocks force-push + deletion only) at the user's explicit choice over two stricter options (required-status-checks, or full PR-only) — chosen specifically to not disrupt the direct-push-to-dev workflow already in use this same session.
- **Built a `gotham` (Batman) theme, a 14th background pattern, and a real speed/opacity/count effect system — user-initiated, not a violation of the "no more themes" non-goal (2026-08-09/10)** — the [[roadmap]] non-goal says "don't propose new theme work unprompted"; this was the user directly asking for a Batman theme, so it doesn't apply. Built in stages, each driven by a follow-up round of user feedback:
  1. New `gotham` preset in `THEMES` (`static/js/theme.js`) — bat-signal amber accent (`red: '#f2c14e'`), deep indigo bg/panel, matching the codebase's existing per-theme-override convention (same shape as `navyseal`, `cyberpunk`, etc.).
  2. A 14th canvas background pattern, **`bats`** — flapping silhouettes on a wandering flight path, built on the exact same engine as the other 13 (`_CANVAS_PATTERNS` dispatcher, `_BG_CLASSES`, cleanup-selector, dropdown `<option>`) — no new pattern-rendering machinery, just one more entry.
  3. **A full speed/opacity/count axis system across all 14 patterns**, in response to the user noticing "Intensity" barely affected the new bats pattern and asking why. Audited all 14 and found the existing single "Intensity" slider actually meant different, inconsistent things per effect — Rain's intensity drove speed + spawn-rate, not opacity (its `d.alpha` was never scaled by intensity at all); Aurora/Waves baked intensity into wave *amplitude* (a size concern) alongside alpha; Matrix's "speed" was secretly `0.3 + intensity*0.7`, with alpha untouched by intensity. Decoupled all of it: Intensity is now opacity-only everywhere; added `applyBgEffectSpeed`/`_getEffectSpeed()` (new `--bg-effect-speed` CSS var, applies to all 14 — even field effects like Aurora/Hexgrid get a real speed knob now, previously none did) and `applyBgEffectCount`/`_getEffectCount()`/a shared `_reconcileCount(arr, target, makeFn)` helper (new `--bg-effect-count` var, live grow/shrink of the particle array each frame — no re-picking the theme needed) for the 10 discrete-particle patterns (bats, rain, petals, sparkles, embers, synapse, constellations, perlin-flow, nebula, ripples); Aurora/Waves/Hexgrid stayed opacity+speed+size only since a field effect has no population to scale, and the Count slider group hides for them (`_COUNT_CAPABLE_PATTERNS` Set gates visibility, mirroring the existing `_STATIC_PATTERNS` hide-for-none/dots pattern). Two new sliders added to the Customize tab UI, full persistence wiring (`_getOpts()`, swatch-click handler, init block, `save()`/`saveCustomTheme()` whitelists) mirroring the existing Size slider's plumbing exactly.
  4. **Gotham-only decorative flourish**, tied to theme *name* rather than the pattern picker (so it shows regardless of which of the 14 patterns is selected) via a new `applyThemeDecor(name)` export that creates/removes a `#gotham-fx` DOM element: a sweeping bat-signal light (`.gotham-fx-beam`, CSS `clip-path` cone + `rotate` keyframe, colored via `var(--red)` so it tracks the theme's own accent) and 3 blurred drifting cloud blobs (`.gotham-fx-cloud`, staggered 95s/130s/160s loops with negative `animation-delay` so they don't start bunched). A bat-logo emblem watermark was also attempted and then explicitly removed by the user: first pass used a hand-drawn CSS-mask SVG path that read as an indistinct blur at low opacity ("it's only a cloud right?"); second pass swapped in the user's own real Batman-logo SVG (`svgwaves_io_batman.svg`, potrace-traced, copied to `static/icons/gotham-bat.svg` and referenced by URL rather than inlined) — user then decided to drop the logo entirely and keep just the beam+clouds, so the emblem CSS/markup/asset were all removed again.
  5. **Real debugging catch worth remembering**: the emblem's first placement (`bottom: -8vh`, meant to peek up from the bottom edge) was invisible because `.chat-input-bar` has `background: var(--panel)` (opaque) and covers the full width at exactly that screen position — confirmed by grepping every opaque-background rule on `.chat-container`/`.chat-history`/`.sidebar`/`.icon-rail`/`.chat-input-bar` before touching CSS a second time, rather than guessing again. The visible "gap" for any fixed decorative/canvas layer is specifically `.chat-container`/`.chat-history`, which have no background set at all (letting `body`'s flat `--bg` + anything `z-index:0` and prepended to `body` show through) — the same reason the canvas patterns themselves are visible there and nowhere else.
  6. Verified via `node --check` on every `theme.js` edit + a CSS brace-balance check on every `style.css` edit, plus a real `docker compose up -d --build odysseus` rebuild-and-visual-check cycle after each round (see [[stack]] — this app's Docker image bakes in the source via `COPY . .` with no bind-mount, so `docker compose restart` alone does **not** pick up code changes). Not yet committed — `static/index.html`, `static/js/theme.js`, `static/style.css` are all modified in the working tree as of this log entry.
- **PR #1 (upstream-sync's first output) merged into dev (2026-08-05)** — the 5 pending upstream commits landed via the new workflow's own output, closing the loop end-to-end: cron/dispatch detects drift → merges cleanly on `upstream-sync` → opens a PR → human reviews and merges. 3 of the repo's PR gate checks failed (Conventional Commits title format, PR description template completeness, `dependency-review` needing "Dependency graph" enabled in repo settings) — all advisory/format gates built for external contributor PRs to the upstream project, not functional problems (tests, CodeQL, and security scans all passed), and `dev` has no branch protection rule enforcing them. Merged directly rather than fabricating template content that wouldn't honestly describe an automated sync. Full suite green after merge — 4814 passed, 3 skipped, 0 failed (up from 4796 pre-merge, consistent with the 5 commits adding coverage).
- **Resolved PR #3's 6 merge-conflict hunks across 4 files by hand, in an isolated git worktree (2026-08-10/11)** — the upstream-sync workflow commits conflict markers as literal text rather than blocking (documented design, see the 2026-08-05 workflow entry), so PR #3 needed manual resolution. Used `git worktree add -b upstream-sync <tmp-path> origin/upstream-sync` specifically to avoid touching the main checkout's own uncommitted gotham-theme work. Every hunk had real, non-overlapping fixes on both sides — merged rather than picking a side: `chat.js` (kept both new imports, no overlap); `subprocess_tools.py` (combined the tmux guard to skip on both Windows *and* sudo commands; routed subprocess launch through upstream's cross-platform `_create_bash_subprocess` while keeping local's sudo-password stdin-piping broker intact — verified `command`, not raw `content`, must be passed since local mutates it via `_inject_sudo_stdin_flags`); `emailInbox.js` (took upstream's request-staleness guard — confirmed required by reading the rest of the function and its call sites, which already depended on `isCurrentOpen()`/`mailboxContext` outside the conflict hunk — and restored local's `ai-reply-full` mode that the upstream hunk had silently dropped); `emailLibrary.js` (took upstream's `mark_seen_failed` handling — confirmed correct because it correctly calls `commitReadState()`/`restoreUnreadState()`, helpers already defined elsewhere in the same function that local's competing hunk skipped — kept local's `_prefetchAdjacentEmails` call, now gated behind the same staleness check). Pushed (`a33d060`), CI came back with exactly one failure: `test_email_open_dedup_js.py`'s "exactly one authoritative request" assertion now counted 2, not because of anything in the resolved hunks but because upstream's merge inserted a whole new function (`_loadReadingPane`, the real 3-pane reading-pane loader with its own legitimate fetch) between `_toggleCardPreview`'s actual closing brace and the doc-comment string the test used as an end-of-function marker — the test's text-slice boundary was just stale, not the code. Verified the fix by replicating the test's exact extraction logic + all 10 assertions in a standalone script before pushing (`3f9777e`) rather than guessing. All CI green afterward, including the real pytest suite (4918 tests) — not yet merged, see [[roadmap]].
- **Built a real `--on-accent` contrast system after the gotham theme exposed a systemic bug (2026-08-11)** — user flagged unreadable white-on-yellow buttons (`.admin-btn-add`, "Open Email Settings" etc.). Investigation found this wasn't gotham-specific: 46 CSS rules in `style.css` pair `background: var(--red)` (or `var(--accent)`) with hardcoded `color: #fff`/`white`, plus 5 more as inline styles across `admin.js`, `document.js`, `settings.js` (×2), and `compare/scoreboard.js` — all fine for every built-in theme's dark/mid-tone red, all broken for a light one. Fixed with a computed token rather than a gotham-only exception (the accent color is user-configurable, including in custom themes, so a hardcoded exception would just recreate the same bug for the next light accent): `_relativeLuminance(hex)` (WCAG formula, gamma-corrected) computes the true contrast ratio of white against `--red` in `applyColors()`, and `--on-accent` is set to a dark near-black below a 3:1 ratio, white above it. Chose WCAG luminance over the simpler HSL-lightness that `hexToHSL()` already provides in this file — verified by hand that they disagree: dark theme's red (`#e06c75`, HSL-L≈65%) and gotham's amber (`#f2c14e`, HSL-L≈63%) have nearly identical HSL lightness but very different WCAG luminance (0.27 vs 0.56), because WCAG weights green far more heavily (0.7152) than red (0.2126) or blue (0.0722), and amber's G channel (193) is much higher than red's (108). Swapped all 46 CSS sites via a script matching exact rule-body spans (not a blind find/replace, to avoid touching unrelated white-text elsewhere) and the 5 JS inline sites individually. One JS site (`models.js`'s "IMG" badge, `background:var(--accent,#7c3aed)`) was deliberately left as `color:#fff` — its background is a fixed purple fallback unrelated to the theme's `--red`, since `--accent`/`--accent-primary` are never actually set anywhere in `theme.js` (confirmed by grep) and always resolve through to that literal fallback; swapping it to `--on-accent` (computed from `--red`, a color this badge never uses) would have been a new bug, not a fix. Side effect: this also fixes pre-existing, unrelated-to-gotham contrast failures on **navyseal** (cyan `#00bcd4`), **terminal** (bright green `#00ff41`), and **cute** (pink `#ff6b9d`) — all three also fail the same 3:1 threshold and were silently broken before this session. Verified live (first time this session with real login access) by injecting a throwaway `.admin-btn-add` element and measuring its actual computed contrast: 10.68:1, up from ~1.68:1.
- **Live browser access (first time this session) turned up two real Email-panel bugs invisible from code-reading alone, plus a false alarm (2026-08-11)** — user screenshotted the tags-toggle button overlapping the search placeholder text and the "Last updated" line looking clipped. (1) Tags overlap: an earlier code-reading-only pass had already bumped the chip-bar's base inline `padding-right` 134→150px, but live DOM measurement showed the *computed* value was still 66px regardless — `getComputedStyle` traced it to a second, `!important`-scoped rule the first pass had missed: `.email-search-wrap.email-reminder-bell-hidden .memory-search-input { padding-right: 66px !important; }`, active whenever the reminder-bell button is hidden (its default state, so the common case). `!important` beats inline styles, so this rule — not the one already "fixed" — was actually in control. Bumped both its desktop (66px) and mobile (132px) variants to 150px; re-measured live afterward and confirmed 0px overlap (was 66px short). Lesson: an inferred fix from code alone can look right and still be wrong if a higher-specificity rule elsewhere wins the cascade — live measurement is what caught it, not more reading. (2) "Last updated" clipping: a stray `position: relative; top: 4px` on `.email-lib-sync-status` pushed it just past the pane's `overflow: hidden` boundary at tighter layouts; removed, confirmed via live bounding-rect measurement (now 13px clear of the pane's bottom edge). (3) False alarm, investigated and explained rather than "fixed": the "New" compose button appearing stranded on the right isn't a bug — flexbox (`margin-left: auto` on the button, `flex: 1` on the currently-empty `#email-lib-accounts`) pushes it there naturally when no accounts are configured yet; resolves itself once an account exists. (4) Separately, user asked why narrowing the window hides a second email-account chip — turned out to be a real discoverability gap, not clipping: `#email-lib-accounts` was already built to scroll horizontally at every width (a code comment confirms this was a deliberate earlier fix, not an oversight), but its scrollbar is intentionally hidden (`scrollbar-width: none`) with zero visual cue that more content exists off-screen. Added a right-edge `mask-image` fade as a standard scroll hint — CSS-only, no JS scroll-state tracking needed, harmless when nothing actually overflows.
- **Mid-session Docker rebuilds disrupted the user's live session and looked like data loss + a hung UI — it wasn't (2026-08-11)** — right after several `docker compose up -d --build` cycles (needed per the existing image-bake-in gotcha, see [[stack]]), the user's browser showed their 2 configured email accounts missing and the email list stuck on loading skeletons. Investigated carefully before concluding anything: `sqlite3 data/app.db "SELECT COUNT(*) FROM email_accounts"` on the host, bypassing the app/API layer entirely, confirmed both accounts were still there (count=2) — not lost, since `data/` is a genuine bind-mount that survives image rebuilds. The apparent hang was explained by measuring `/api/email/accounts` directly: 3167ms on the first request after the restart, 10ms on the very next one — a one-time cold-start tax (first-request lazy imports/connections), not a persistent bug, that happened to land exactly when the user was live-testing. The accounts still showed empty even on the fast second call, which is a session/owner-scoping side effect of the container restart (the browser tab's session predates the new process) rather than a data problem — a plain page refresh clears it. Net: no code bug found or needed here, but worth remembering as a real operational cost of rebuild-to-verify cycles landing mid-user-testing, not something to repeat carelessly.
- **Email settings page couldn't be closed — two separate bugs, plus a wrong first fix worth recording (2026-08-11/12)** — user reported being unable to exit the Email settings page. Two independent causes: (1) the gear button (`#email-lib-settings-btn`) called `_showEmailSettingsPage()` unconditionally, so a second click re-fetched and re-rendered the same page instead of closing it — clearly half-finished rather than intentional, since the button already sets `.active` and `aria-expanded`, both of which promise collapse-on-second-click. Made it a real toggle. (2) Escape closed the *entire* Email window rather than backing out of settings. **The first attempt at (2) was wrong and shipped before being verified** — a branch added to `emailLibrary.js`'s own `state._libEscHandler` that could never execute. Root cause found by monkey-patching `Event.prototype.stopPropagation`/`stopImmediatePropagation` and capturing stack traces: `ui.js` (~line 1324) registers a **document-capture** keydown handler that resolves the top modal, clicks its close button, and calls `stopImmediatePropagation()` — it runs first and terminates the event, so no module-level Escape branch downstream of it can ever run. Fixed properly *in `ui.js`*, mirroring the `settingsModal` inner-form case sitting ~10 lines above it in the same function (which already does exactly this one-level-retreat pattern), and clicking the existing `#email-settings-header-back` button so all show/hide bookkeeping stays in one place; the dead `emailLibrary.js` branch was deleted rather than left as misleading code. Verified live end-to-end: gear opens → gear closes → gear reopens → Escape closes settings with Email still open → second Escape closes Email. **Generalizable lesson**: `ui.js`'s global Escape handler is the authority for modal-level Escape in this app — any per-modal Escape behavior has to be added *there*, not in the module, or it silently never runs.
- **Account chips: "both look active" and "clicking sometimes does nothing" — two real bugs behind one vague report (2026-08-11/12)** — user said the account chips "act weird"; asked rather than guessed, and got two concrete symptoms that turned out to have separate causes. (1) **Both chips looked selected**: `.email-account-chip.email-account-has-unread` (bg `--red` 10%, border 42%, color 86%) is visually almost identical to `.gallery-chip.active` (bg 15%, border 40%, color 100%) — *and*, being declared much later in `style.css` at equal specificity (0-2-0), it actually **overrode** `.active` outright. So any account with unread mail rendered exactly like the one being viewed, and with both accounts unread there was no way to tell which was active. Fixed by making unread a mild lift only (the leading dot + trailing count already carry that signal) and giving the selected chip a deliberately stronger treatment (solid accent border, 22% fill, weight 600), ID-scoped as `#email-lib-accounts .email-account-chip.active` (1-2-0) so it can't lose the cascade to a later equal-specificity rule again. (2) **Dead click zone**: the set-default dot is a *sibling* `<button>` absolutely positioned over the chip's reserved `padding-right:24px`, and its handler calls `e.stopPropagation()` — so clicking a chip's right edge never reached the chip's own select handler, and on the account that was *already* default, set-default is a no-op, making that zone do literally nothing. Fixed by having the dot also select the account when it isn't the active one, so no part of a chip is dead. Both verified live by measuring computed styles and clicking through real DOM (re-querying after each click — an initial test wrongly reported failure because `_renderAccountsStrip()` replaces `innerHTML` and the captured element references had gone stale). Ruled out along the way, rather than assumed: account IDs are UUID strings (`sqlite3` on the real DB), so there was no string-vs-number identity mismatch; and `is_default` is correctly one-per-owner, so duplicate defaults weren't the cause either.
- **Test hygiene note — set-default clicks mutate real user data (2026-08-11/12)** — verifying the chip dead-zone fix required clicking a real "set default account" control, which writes to `email_accounts.is_default`. Followed the baseline-snapshot protocol adopted after the 2026-07-20 RSS incident: captured the pre-test state with `sqlite3`, and confirmed afterward that the two test clicks had flipped owner `yahya`'s default and flipped it back (net zero, original row restored). The user's own owner (`admin`) was never touched. Also settled a recurring confusion: the preview browser session is authenticated as owner **`yahya`** (BearCode + Personal) while the user's browser is **`admin`** (Yahya + BearCode) — which is why `/api/email/accounts` looked "empty" or wrong from the agent side. Different users, not a bug; check `owner` before concluding anything about account data.
- **Search provider API keys couldn't be entered — `/api/search/providers` regressed to a trimmed payload (2026-08-16)** — user reported being unable to add keys for Firecrawl and Exa. Not a UI bug and not specific to those two: `routes/search/search_routes.py`'s `list_search_providers()` was iterating the **legacy** `PROVIDER_INFO` 3-tuple map (`label, needs_key, needs_url`) and returning only `{id, label, available}`, dropping `needs_key`, `needs_url`, `key_setting`, `env_var`, `hint`, and `has_additional`. `static/js/settings.js` gates the API-key row on `info.needs_key` (line ~1189) and writes to `info.key_setting`, so an absent `needs_key` read as falsy and the key input was hidden for **every** key-requiring provider — brave, google_pse, tavily, serper, bing, search1api, firecrawl, exa (8 of 10), plus Google PSE's extra CX field which comes from `has_additional`. The registry itself was intact (`PROVIDER_REGISTRY` in `services/search/providers.py` still had all 10 providers with full `ProviderInfo`), so this was purely the endpoint serializing the wrong source — consistent with the route having been moved into the `routes/search/` subpackage during an upstream refactor and the richer response not surviving the move. Fixed by iterating `PROVIDER_REGISTRY` and emitting the full metadata; verified live against the running app that `exa`/`firecrawl` now return `needs_key: true` with their `key_setting`/`hint`, and that all 8 key-requiring providers would render the input. **Worth noting as a pattern**: this is the second time an upstream merge has quietly reverted local search-provider work (see the 2026-06-25 entry where upstream's `providers.py` had gone back to a 6-provider tuple dict) — the provider registry and this endpoint are a recurring merge casualty and deserve an explicit check after every upstream sync.
- **`upstream-sync.yml` has been failing every night since 2026-08-12 — the PAT lacks workflow scope (2026-08-16)** — five consecutive scheduled runs failed (31572262335, 31676347600, 31778374697, 31868984014, 31931038222), with no open PR and 53 upstream commits now queued. The merge step behaving "badly" is a red herring: `Automatic merge failed; fix conflicts` is the workflow's documented design (it commits conflict markers and opens a draft PR). The actual failure is the push: `! [remote rejected] upstream-sync -> upstream-sync (refusing to allow a Personal Access Token to create or update workflow '.github/workflows/ci.yml' without 'workflow' scope)`. This upstream batch touches `.github/workflows/ci.yml`, and GitHub refuses any PAT-authenticated push that adds or modifies a file under `.github/workflows/` unless the token carries workflow permission — classic PAT scope, or "Workflows: Read and write" on a fine-grained PAT like `SYNC_PR_TOKEN`. Nothing in the repo can work around this by having the *same* token push harder; the options are (a) grant the PAT that permission, (b) push with the default `GITHUB_TOKEN` (which can update workflow files when the job grants `contents: write`) while still creating the PR with the PAT — the PAT is only needed for PR creation, per the 2026-08-05 entry — or (c) exclude `.github/workflows/**` from the sync, which would silently skip upstream CI improvements and is the worst of the three. **Requires a decision + a token change by the maintainer; not fixable from inside the repo alone.**
- **First upstream review landed on PR #6108, and it was right — one global `--on-accent` was too coarse (2026-08-19)** — reviewer `RaresKeY` requested changes on the contrast PR, and the finding held up on inspection. `--on-accent` was set in `applyColors()` from `colors.red`, but the advanced-override loop (`ADV_KEYS`) runs *further down the same function*, so `--send-btn-bg` / `--send-btn-hover` could resolve to a colour with no relation to `--red` while the foreground still reflected `--red`. Their reproduction was exact: `--red: #e06c75` with `--send-btn-bg: #f2c14e` left the token at `#fff` for 1.68:1 on the actual button. Four rules were affected — `.send-btn`, `.send-btn:hover`, `.send-btn.newchat-mode:hover`, and `.ge-ai-command-run`. Fixed by modelling each independently configurable surface as a background/foreground pair resolved *after* the overrides: `--on-accent` (from `--red`, for consumers genuinely painted with it), `--on-send-btn`, `--on-send-btn-hover`, and `--on-send-btn-newchat-hover`. The last one needed its own token because that background is itself a `color-mix()` rather than a flat variable, so a small `_mixSrgb()` reproduces the CSS mix and the surface is measured rather than assumed; a shared `_readableOn(bg)` now does the white-vs-near-black decision for every surface. Added `tests/test_theme_accent_foreground_js.py`: one test runs the helpers under Node with the reviewer's exact dark-accent/light-send-button case and asserts the naive shared foreground would fall below 3:1 while each real pairing clears it, and a second scans `style.css` so no rule can pair `--on-accent` with a send-button background again. **Lesson worth keeping:** a derived token is only valid for the backgrounds it was derived from — when a surface's background is independently themeable, its foreground has to be derived per-surface, and the check has to measure the *rendered* control rather than the source variable.
- **Fork PRs to `odysseus-dev` need maintainer approval for every push (2026-08-19)** — after pushing the review fix, only 3 checks reported (`Check PR description`, `Check PR title`, `Flag unmergeable PRs`). Those are the `pull_request_target` workflows, which run in the base-repo context. The 7 heavyweight ones (`CI`, `CodeQL`, `Trivy`, `Container scan`, `Dependency review`, `Secret scan`, `Workflow security`) are plain `pull_request` workflows and come back `action_required` — GitHub gates fork contributions behind a maintainer click on each new push, not once per PR. So "only 3 checks green" on a fork PR is normal and not a regression to chase; check `gh run list --branch <branch>` for `action_required` before assuming something broke.
- **The upstream sync needed two fixes, and the first one was mine to un-break (2026-08-23 / 2026-09-01)** — the sync had been failing every night since 2026-08-18 because `SYNC_PR_TOKEN` lacked the `workflow` permission and one upstream commit edited `pr-description-check.yml`. The permission was granted (option (a) from the 2026-08-16 note), which fixed it immediately. PR #5 then hardened the workflow by dropping `.github/workflows/` from the sync branch entirely, so the sync would never again depend on token scope. That was the wrong trade. Upstream ships tests that assert a workflow file exists or contains certain lines, so withholding those files just moved the failure from the sync run to the sync PR's CI: `test_pr_description_check.py` failed on 2026-08-23 against the fork's older copy, and on 2026-08-30 `test_docs_no_orphan_images.py` failed on `deploy-pages.yml`, a file newly added upstream that had no fork copy to fall back to. That run silently withheld 11 workflow files. PR #8 replaced the blanket skip with a real push attempt whose fallback keys off the actual rejection text, so behaviour follows what the token can do rather than a standing assumption about it. The recoverable case is deliberately narrow and was checked against five rejection strings: only the workflow-scope guardrail falls back, while a protected branch, a non-fast-forward, a network error and a revoked token all still fail the step. **Lesson worth keeping:** trading a loud failure for a silent one is usually a downgrade, and "we already carry our own copy" stops being true the moment upstream adds a file you have never seen.
- **`codeql.yml` is deliberately absent, and git resolves that conflict the wrong way (2026-09-01)** — the 6-commit sync produced one conflict, a modify/delete on `.github/workflows/codeql.yml`: deleted on this fork's `dev`, modified upstream. Modify/delete conflicts leave no markers, so a marker grep reported the tree clean while git had quietly left **upstream's** version in place, which `git add -A` then committed. The deletion is intentional: this repo has CodeQL **default setup** configured since 2026-08-05 (actions/javascript/python/swift/typescript, extended query suite), which is why `Analyze (swift)` runs on every PR despite there being no Swift in the tree, and GitHub does not allow default setup and an advanced-setup workflow to coexist. Upstream has no default setup so it still ships the file. Resolved by keeping the deletion; expect the same conflict on any future sync that touches it. **Also worth keeping:** "no conflict markers" does not mean "no conflicts" — modify/delete and rename conflicts never produce markers.
- **`git fetch origin <branch>` does not update `origin/<branch>`, and it cost real work twice (2026-08-23)** — a plain `git fetch origin upstream-sync` writes `FETCH_HEAD` but leaves the remote-tracking ref untouched unless a refspec is configured for it. Building a worktree from `origin/upstream-sync` therefore checked out a **stale sync branch from an earlier era**, and a full five-file conflict resolution was done against a commit that was 93 commits behind the real tip and no longer existed on the remote. The real tip had one conflict, not five. The same gap then made `--force-with-lease` refuse with "stale info", because the lease has no tracking ref to compare against. Fix is an explicit refspec (`git fetch origin '+refs/heads/X:refs/remotes/origin/X'`) or `--force-with-lease=branch:sha`. **Verify the tip with `git ls-remote` before resolving anything on a branch someone else force-pushes.**
- **Tool visibility was gated on the union of the category sections, which left rail launchers visible (2026-08-23)** — `resolveVisibility()` kept every tool visible whenever *any* of `tools-section`, `ai-tools-section` or `personal-section` was on. That union was a stand-in for the 2026-07-24 restructure, which split upstream's single `#tools-section` into categories; `#tools-section` has no node in this fork at all, so gating on it alone hid everything. The cost was real and not just a failing test: switching one category off left that category's icon-rail launchers visible, because the full sidebar hides children through the section container while the rail has no containers. Replaced with a per-tool parent map (`UI_VIS_TOOL_PARENT`) built from where the buttons actually sit in `index.html`, so a tool needs both the master toggle and its own category. This also settled the two upstream tests that arrived with the sync, which encode the same rule. Verified by calling the real module rather than only through pytest: `personal-section` off now hides calendar and keeps research, `ai-tools-section` off does the reverse, master off hides both, and email is unaffected throughout.
- **A test's brace walker treated apostrophes in comments as string delimiters (2026-08-23)** — `test_email_library_prewarm.py::_function_source()` tracked strings and template literals but not comments, so a `//` comment containing "isn't" or "don't" opened a phantom string that swallowed every brace until the next apostrophe. The walk then ran to EOF at depth 20 and raised `unterminated function openEmailLibrary`. There are 89 such comment lines inside that function in this fork and none upstream, which is exactly why the helper passes there and fails here; `node --check` parses the file fine, so the JS was never at fault. Fixed by skipping `//` and `/* */` runs before any brace or quote accounting. Same shape as the 2026-08-13 `test_email_open_dedup_js.py` end-marker problem: **source-scanning tests calibrated on upstream's prose break on this fork's, and the failure always looks like a syntax error in code that is fine.**
- **PR #6108's second review: a 3:1 floor was the wrong target, and white-vs-`#171717` cannot reach the right one (2026-09-01)** — maintainer `pewdiepie-archdaemon` pointed out that the derived tokens land on button labels of roughly 9 to 15px, which WCAG treats as normal-size text at 4.5:1; the 3:1 value applies to large text and non-text UI components. He also predicted a middle-luminance band where neither candidate clears AA, and the maths confirms it exactly: white clears 4.5:1 only up to a background luminance of 0.1833 and `#171717` only from 0.2136, leaving everything in between with no AA-passing answer. Pure black closes it, since the better of white and black is never worse than 4.583:1. `_readableOn()` now measures both ends against the resolved background and takes the higher, keeping `#171717` only where it still clears AA so the softer look survives where it is allowed. Checked by walking the colour cube rather than trusting the derivation: 636,056 backgrounds, none below 4.5:1, worst case exactly 4.500:1. **One visible consequence:** the stock accent `#e06c75` gives white only 3.20:1, which passed the old floor and fails AA, so default-theme button text now resolves to `#171717` at 5.61:1. That is the same problem as issue #6109, but it changes how the app looks out of the box, so it was raised explicitly for a maintainer decision rather than absorbed. It surfaced because the test fixture was leaning on that colour.
- **Screenshotting the app needs a headless browser and an injection proxy, not the Browser pane (2026-08-20)** — refreshing PR #6108's screenshots failed repeatedly: the Browser pane returns "the page is not compositing frames" whenever it is not displayed, and `brave --headless --screenshot` hangs on this app because an open SSE stream means the load event never settles and `--virtual-time-budget` never expires. What works is Playwright (already cached under `~/.npm/_npx`) driving Brave as the Chromium binary, with `waitUntil: 'domcontentloaded'` plus an explicit sentinel selector. Getting the app into a known theme state needed a second trick: a small reverse proxy on another port that injects a `<script>` into `<head>` to seed `localStorage` **before** the app's own scripts run, keeping the same origin so nothing else changes. Also worth knowing: a CSS transition frozen mid-flight reports `oklab(...)` from `getComputedStyle` and will mislead any measurement, so set `transition: none` before reading colours.
- **PR body and CI notes for this fork (2026-08-23)** — two recurring frictions. The `Check PR description` bot requires a linked issue reference, so a fork-local CI fix with no issue can use `Part of #N` pointing at the related PR. And CodeQL's `Analyze (swift)` is consistently the slowest check on this repo by a wide margin, often leaving a PR `UNSTABLE` for many minutes after everything else is green; it is not a failure to chase.
